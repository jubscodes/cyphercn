---
phase: 02.4-aesthetic-compliance-review
plan: 03
type: execute
wave: 2
depends_on: ["02.4-01"]
files_modified:
  - components/ui/cypher/blocks/main-menu.tsx
  - components/ui/cypher/blocks/game-over.tsx
  - components/ui/cypher/blocks/save-slots.tsx
  - components/ui/cypher/blocks/loading-screen.tsx
autonomous: true

must_haves:
  truths:
    - "main-menu.tsx uses terminal-appropriate copy instead of 'Retro 8-bit Quest'"
    - "game-over.tsx uses ASCII skull art instead of /images/8-bit-skull.png"
    - "save-slots.tsx uses cyphercn class instead of retro class"
    - "loading-screen.tsx uses cyphercn class instead of retro class"
  artifacts:
    - path: "components/ui/cypher/blocks/main-menu.tsx"
      provides: "Terminal-styled main menu block"
      contains: "COMMAND CENTER"
    - path: "components/ui/cypher/blocks/game-over.tsx"
      provides: "Game over screen with ASCII skull"
      contains: "ASCII skull"
    - path: "components/ui/cypher/blocks/save-slots.tsx"
      provides: "Save system with terminal styling"
      contains: "cyphercn"
    - path: "components/ui/cypher/blocks/loading-screen.tsx"
      provides: "Loading screen with terminal styling"
      contains: "cyphercn"
  key_links:
    - from: "components/ui/cypher/blocks/save-slots.tsx"
      to: "cyberpunk.css"
      via: "cyphercn class"
      pattern: "cyphercn"
---

<objective>
Update game-focused block components to comply with MS-DOS terminal aesthetic: replace non-terminal copy, remove image references, and update class names from "retro" to "cyphercn".

Purpose: These blocks demonstrate the library's aesthetic and must exemplify terminal styling without 8-bit/retro/pixel terminology.
Output: Four block components with terminal-appropriate copy and styling.
</objective>

<execution_context>
@/Users/jubs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jubs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@components/ui/cypher/blocks/main-menu.tsx
@components/ui/cypher/blocks/game-over.tsx
@components/ui/cypher/blocks/save-slots.tsx
@components/ui/cypher/blocks/loading-screen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update main-menu.tsx copy and save-slots.tsx classes</name>
  <files>components/ui/cypher/blocks/main-menu.tsx, components/ui/cypher/blocks/save-slots.tsx</files>
  <action>
**main-menu.tsx:**
Line 40 - Change the CardDescription from "Retro 8-bit Quest" to "COMMAND CENTER":
```tsx
<CardDescription>COMMAND CENTER</CardDescription>
```

This is the only change needed in main-menu.tsx.

**save-slots.tsx:**
Replace all instances of "retro" class with "cyphercn" or "cyphercn-normal":

Lines to update (use cyphercn for uppercase text, cyphercn-normal for normal case):
- Line 134: `className="retro text-[8px]..."` -> `className="cyphercn-normal text-[8px]..."`
- Line 148: `className="retro text-[8px]..."` -> `className="cyphercn-normal text-[8px]..."`
- Line 210: `"retro text-[10px]..."` -> `"cyphercn text-[10px]..."` (this is a label, use uppercase)
- Line 217: `className="...retro shrink-0..."` -> `className="...cyphercn shrink-0..."` (badge, uppercase)
- Line 231: `className="...retro"` -> `className="...cyphercn-normal"` (description, normal case)
- Line 239: `className="...retro"` -> `className="...cyphercn-normal"` (timestamp, normal case)
- Line 302: `className="retro text-sm"` -> `className="cyphercn text-sm"` (message, uppercase)
- Line 356: `className={cn("...retro")}` -> `className={cn("...cyphercn-normal")}` (footer text)

Also:
- Line 141: Remove `pixelated` from className if present (use image-rendering CSS property sparingly)

Total: 1 change in main-menu.tsx, ~9 changes in save-slots.tsx
  </action>
  <verify>
- Grep for "Retro 8-bit" in main-menu.tsx - should find 0 matches
- Grep for "COMMAND CENTER" in main-menu.tsx - should find 1 match
- Grep for "retro" in save-slots.tsx - should find 0 matches
- Grep for "cyphercn" in save-slots.tsx - should find 8+ matches
- Run `npx tsc --noEmit` - no errors
  </verify>
  <done>
- main-menu.tsx displays "COMMAND CENTER" instead of "Retro 8-bit Quest"
- save-slots.tsx uses cyphercn/cyphercn-normal classes throughout
- No "retro" class references remain
- Both components compile without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace game-over.tsx skull image with ASCII art</name>
  <files>components/ui/cypher/blocks/game-over.tsx</files>
  <action>
Line 197 references `/images/8-bit-skull.png`. Replace the img element with ASCII skull art.

Find the img element around line 197:
```tsx
<img
  src="/images/8-bit-skull.png"
  ...
/>
```

Replace with ASCII skull art using box-drawing/text characters:
```tsx
<pre className="text-foreground font-mono text-xs leading-none">
{`    ___
   /   \\
  | x x |
  |  ^  |
  | \\_/ |
   \\___/`}
</pre>
```

Or a more detailed version if space allows:
```tsx
<pre className="text-foreground font-mono text-[10px] leading-tight">
{`     .-""""-.
    /        \\
   | \\      / |
   |  0    0  |
   |    __    |
   |   /  \\   |
    \\ \\__/ /
     '----'`}
</pre>
```

Choose the version that fits the existing layout. The ASCII skull should:
- Use monospace font (font-mono)
- Match the existing container width
- Use currentColor/text-foreground for theme compatibility

Remove the pixelated image reference entirely.
  </action>
  <verify>
- Grep for "8-bit-skull" in game-over.tsx - should find 0 matches
- Grep for "<pre" in game-over.tsx - should find 1+ matches (ASCII art)
- Run `npx tsc --noEmit` - no errors
  </verify>
  <done>
- game-over.tsx displays ASCII skull art instead of PNG image
- No reference to "/images/8-bit-skull.png"
- ASCII art uses terminal-appropriate styling
- Component compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Update loading-screen.tsx classes from retro to cyphercn</name>
  <files>components/ui/cypher/blocks/loading-screen.tsx</files>
  <action>
Replace all instances of "retro" class with "cyphercn" or "cyphercn-normal":

Lines to update:
- Line 97: `"retro text-xl md:text-2xl..."` -> `"cyphercn text-xl md:text-2xl..."` (title, uppercase)
- Line 108: `className="retro text-xs..."` -> `className="cyphercn-normal text-xs..."` (percentage, normal)
- Line 124: `className="retro text-[0.625rem]..."` -> `className="cyphercn-normal text-[0.625rem]..."` (loading tip, normal case)

Total: 3 changes
  </action>
  <verify>
- Grep for "retro" in loading-screen.tsx - should find 0 matches
- Grep for "cyphercn" in loading-screen.tsx - should find 3+ matches
- Run `npx tsc --noEmit` - no errors
  </verify>
  <done>
- loading-screen.tsx uses cyphercn/cyphercn-normal classes
- No "retro" class references remain
- Component compiles without errors
  </done>
</task>

</tasks>

<verification>
```bash
# Verify no retro/8-bit terminology
grep -rn "retro\|8-bit\|8bit" components/ui/cypher/blocks/main-menu.tsx components/ui/cypher/blocks/game-over.tsx components/ui/cypher/blocks/save-slots.tsx components/ui/cypher/blocks/loading-screen.tsx
# Expected: 0 matches

# Verify cyphercn classes added
grep -c "cyphercn" components/ui/cypher/blocks/save-slots.tsx
# Expected: 8+

grep -c "cyphercn" components/ui/cypher/blocks/loading-screen.tsx
# Expected: 3+

# Verify ASCII skull
grep -c "<pre" components/ui/cypher/blocks/game-over.tsx
# Expected: 1+

# TypeScript check
npx tsc --noEmit
```
</verification>

<success_criteria>
- main-menu.tsx: Displays "COMMAND CENTER" instead of "Retro 8-bit Quest"
- game-over.tsx: ASCII skull art replaces /images/8-bit-skull.png
- save-slots.tsx: All "retro" classes replaced with cyphercn/cyphercn-normal
- loading-screen.tsx: All "retro" classes replaced with cyphercn/cyphercn-normal
- All four components compile without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/02.4-aesthetic-compliance-review/02.4-03-SUMMARY.md`
</output>
