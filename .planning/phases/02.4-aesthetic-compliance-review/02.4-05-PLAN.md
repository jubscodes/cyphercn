---
phase: 02.4-aesthetic-compliance-review
plan: 05
type: execute
wave: 3
depends_on: ["02.4-02", "02.4-03", "02.4-04"]
files_modified:
  - components/ui/cypher/blocks/victory-screen.tsx
  - components/ui/cypher/blocks/chapter-intro.tsx
  - components/ui/cypher/blocks/README-player-profile-card.md
  - components/ui/cypher/alert-dialog.tsx
autonomous: false

must_haves:
  truths:
    - "victory-screen.tsx uses cyphercn class instead of retro"
    - "chapter-intro.tsx removes pixelated image rendering"
    - "README-player-profile-card.md uses terminal terminology"
    - "alert-dialog.tsx comment updated from 'Pixelated border' to 'Terminal border'"
    - "Build passes with no errors"
    - "Visual audit confirms terminal aesthetic across all updated components"
  artifacts:
    - path: "components/ui/cypher/blocks/victory-screen.tsx"
      provides: "Victory screen with terminal styling"
      contains: "cyphercn"
    - path: "components/ui/cypher/blocks/chapter-intro.tsx"
      provides: "Chapter intro without pixelated rendering"
    - path: "components/ui/cypher/blocks/README-player-profile-card.md"
      provides: "Updated documentation with terminal terminology"
      contains: "terminal"
  key_links:
    - from: "all updated components"
      to: "cyberpunk.css"
      via: "cyphercn class"
      pattern: "cyphercn"
---

<objective>
Complete the aesthetic compliance review by updating remaining block components, documentation, and performing a final visual verification checkpoint.

Purpose: Ensure 100% compliance across all cypher components and blocks with the MS-DOS terminal aesthetic.
Output: Remaining components updated + visual audit confirmation.
</objective>

<execution_context>
@/Users/jubs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jubs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02.4-aesthetic-compliance-review/02.4-01-SUMMARY.md
@.planning/phases/02.4-aesthetic-compliance-review/02.4-02-SUMMARY.md
@.planning/phases/02.4-aesthetic-compliance-review/02.4-03-SUMMARY.md
@.planning/phases/02.4-aesthetic-compliance-review/02.4-04-SUMMARY.md
@components/ui/cypher/blocks/victory-screen.tsx
@components/ui/cypher/blocks/chapter-intro.tsx
@components/ui/cypher/alert-dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update remaining blocks and alert-dialog comment</name>
  <files>components/ui/cypher/blocks/victory-screen.tsx, components/ui/cypher/blocks/chapter-intro.tsx, components/ui/cypher/alert-dialog.tsx</files>
  <action>
**victory-screen.tsx:**
1. Line 113: Change `className="...retro"` to `className="...cyphercn"` on ItemTitle (uppercase title)

Total: 1 change in victory-screen.tsx

**chapter-intro.tsx:**
1. Line 47: Remove `style={{ imageRendering: "pixelated" }}` from the element
   - The terminal aesthetic uses clean monospace rendering, not pixelated
   - If the image needs special rendering, use standard CSS or leave default

Total: 1 change in chapter-intro.tsx

**alert-dialog.tsx:**
1. Line 154: Change comment from `{/* Pixelated border */}` to `{/* Terminal border */}`
   - This is a comment-only change for terminology consistency

Total: 1 change in alert-dialog.tsx
  </action>
  <verify>
- Grep for "retro" in victory-screen.tsx - should find 0 matches
- Grep for "pixelated" in chapter-intro.tsx - should find 0 matches
- Grep for "Terminal border" in alert-dialog.tsx - should find 1 match
- Run `npx tsc --noEmit` - no errors
  </verify>
  <done>
- victory-screen.tsx: Uses cyphercn class
- chapter-intro.tsx: No pixelated image rendering
- alert-dialog.tsx: Comment uses terminal terminology
- All compile without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Update README-player-profile-card.md documentation</name>
  <files>components/ui/cypher/blocks/README-player-profile-card.md</files>
  <action>
Update the README to use terminal terminology throughout:

1. Line 3: Change "8bitcn UI library" to "CypherCN terminal UI library"
   Change "retro, 8-bit style" to "MS-DOS terminal style"

2. Line 7: Change "Retro 8-bit Design" to "Terminal Design"
   Change "pixel borders and retro fonts" to "thin borders and monospace fonts"

3. Line 8: Change "8bitcn Avatar component with pixel frame styling" to "CypherCN Avatar component with framed styling"

4. Line 9: Change "retro styling" to "terminal styling"

5. Line 32: Change bio example from "pixel warrior" to "command line warrior" or "terminal operator"

6. Line 118: Change `variant?: "retro" | "default"` to `variant?: "terminal" | "default"` (if this is a prop)

7. Lines 144-150: Update design system description:
   - "8bitcn design system" -> "CypherCN design system"
   - "Retro Font" -> "Terminal Font"
   - "retro font class" -> "cyphercn class"
   - "Pixel Borders" -> "Thin Borders"
   - "8bitcn Card component with pixel borders" -> "CypherCN Card component with thin borders"
   - "8bitcn Progress component with retro variant" -> "CypherCN Progress component"
   - "8bitcn Avatar component with pixel frame styling" -> "CypherCN Avatar component with framed variant"
   - "8bitcn Badge component" -> "CypherCN Badge component"

8. Line 174: Change bio from "pixel warrior" to "terminal operator"

9. Lines 204-215: Update dependencies section:
   - "Avatar component with pixel styling" -> "Avatar component with terminal styling"
   - "Card component with retro borders" -> "Card component with thin borders"
   - "@8bitcn/player-profile-card" -> "@cyphercn/player-profile-card"

10. Line 220: Change "8bitcn UI library" to "CypherCN terminal UI library"
  </action>
  <verify>
- Grep for "8bit\|8-bit\|retro\|pixel" (case insensitive) in README-player-profile-card.md - should find 0 matches
- Grep for "CypherCN\|terminal\|cyphercn" in README-player-profile-card.md - should find multiple matches
  </verify>
  <done>
- README uses CypherCN branding throughout
- All 8-bit/retro/pixel terminology replaced with terminal equivalents
- Installation commands reference @cyphercn/ namespace
  </done>
</task>

<task type="auto">
  <name>Task 3: Run final build verification</name>
  <files>N/A (verification only)</files>
  <action>
Run the build to ensure all changes compile correctly:

```bash
npm run build
```

If build fails, identify and fix any issues before proceeding to visual checkpoint.
  </action>
  <verify>
- `npm run build` completes without errors
- No TypeScript errors in updated files
  </verify>
  <done>
- Build passes successfully
- All updated components compile
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete aesthetic compliance review across all cypher components:
    - Avatar: pixel frame replaced with thin double border, variant renamed to "framed"
    - Icons: 10 icons updated from "retro" to "cyphercn" class
    - Spinner: Animation classes renamed from pixel-* to glyph-*
    - Kbd: Updated from "retro" to "cyphercn" class
    - Blocks: All "retro" classes replaced, Avatar variants updated
    - game-over: ASCII skull art replaces PNG image
    - main-menu: "COMMAND CENTER" replaces "Retro 8-bit Quest"
    - Documentation: README updated with terminal terminology
  </what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Navigate to component showcase pages and verify:
       - Avatar with variant="framed" shows thin double border (not pixelated blocks)
       - main-menu block shows "COMMAND CENTER" text
       - game-over block shows ASCII skull (not PNG image)
       - All text uses monospace font (IBM Plex Mono)
       - No visual regressions in any updated components
    3. Check browser console for any runtime errors
    4. Verify Avatar works correctly in friend-list, character-sheet, player-profile-card, leaderboard, dialogue blocks
  </how-to-verify>
  <resume-signal>Type "approved" if visual audit passes, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
```bash
# Final comprehensive check for non-compliant terminology
grep -rn "retro\|8-bit\|8bit" components/ui/cypher/ --include="*.tsx" | grep -v "font-retro" | head -20
# Expected: 0 matches (or only false positives in comments)

# Check for pixel variant references
grep -rn 'variant="pixel"' components/ui/cypher/
# Expected: 0 matches

# Verify build passes
npm run build
# Expected: Success
```
</verification>

<success_criteria>
- All remaining blocks updated with terminal terminology
- README documentation fully updated
- Build passes without errors
- Human visual verification confirms:
  - Avatar framed variant renders correctly
  - ASCII skull displays properly
  - All text styling consistent
  - No visual regressions
</success_criteria>

<output>
After completion, create `.planning/phases/02.4-aesthetic-compliance-review/02.4-05-SUMMARY.md`
</output>
