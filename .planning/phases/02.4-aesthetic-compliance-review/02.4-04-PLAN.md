---
phase: 02.4-aesthetic-compliance-review
plan: 04
type: execute
wave: 2
depends_on: ["02.4-01"]
files_modified:
  - components/ui/cypher/blocks/friend-list.tsx
  - components/ui/cypher/blocks/character-sheet.tsx
  - components/ui/cypher/blocks/player-profile-card.tsx
  - components/ui/cypher/blocks/leaderboard.tsx
  - components/ui/cypher/blocks/dialogue.tsx
autonomous: true

must_haves:
  truths:
    - "All five blocks use cyphercn/cyphercn-normal instead of retro class"
    - "Avatar variant='pixel' references changed to variant='framed'"
    - "No font-retro class references remain"
  artifacts:
    - path: "components/ui/cypher/blocks/friend-list.tsx"
      provides: "Friends list with terminal styling"
      contains: "cyphercn"
    - path: "components/ui/cypher/blocks/character-sheet.tsx"
      provides: "Character stats with terminal styling"
      contains: "variant=\"framed\""
    - path: "components/ui/cypher/blocks/player-profile-card.tsx"
      provides: "Player profile with terminal styling"
      contains: "variant=\"framed\""
    - path: "components/ui/cypher/blocks/leaderboard.tsx"
      provides: "Leaderboard with terminal styling"
      contains: "variant=\"framed\""
    - path: "components/ui/cypher/blocks/dialogue.tsx"
      provides: "Dialogue box with terminal styling"
      contains: "variant=\"framed\""
  key_links:
    - from: "components/ui/cypher/blocks/*.tsx"
      to: "components/ui/cypher/avatar.tsx"
      via: "Avatar component import"
      pattern: "variant=\"framed\""
---

<objective>
Update player-focused block components to comply with MS-DOS terminal aesthetic: replace "retro" classes with "cyphercn", update Avatar variant from "pixel" to "framed", and remove font-retro references.

Purpose: These blocks showcase player/character data and must use consistent terminal styling to match the established aesthetic.
Output: Five block components with terminal-appropriate class names and Avatar variant usage.
</objective>

<execution_context>
@/Users/jubs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jubs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02.4-aesthetic-compliance-review/02.4-01-SUMMARY.md
@components/ui/cypher/blocks/friend-list.tsx
@components/ui/cypher/blocks/character-sheet.tsx
@components/ui/cypher/blocks/player-profile-card.tsx
@components/ui/cypher/blocks/leaderboard.tsx
@components/ui/cypher/blocks/dialogue.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update friend-list.tsx and character-sheet.tsx</name>
  <files>components/ui/cypher/blocks/friend-list.tsx, components/ui/cypher/blocks/character-sheet.tsx</files>
  <action>
**friend-list.tsx:**
1. Line 125: Change `variant="pixel"` to `variant="framed"` on Avatar
2. Line 129: Change `className="retro text-[5px]..."` to `className="cyphercn text-[5px]..."` (fallback, uppercase)
3. Line 136: Change `className="...retro text-xs..."` to `className="...cyphercn text-xs..."` (name, uppercase)
4. Line 139: Change `className="...retro sm:hidden..."` to `className="...cyphercn sm:hidden..."` (mobile name)
5. Line 158: Change `"uppercase font-retro mr-1..."` to `"uppercase cyphercn mr-1..."` (status badge)

Total: 5 changes in friend-list.tsx

**character-sheet.tsx:**
1. Line 155: Change `variant="pixel"` to `variant="framed"` on Avatar
2. Line 193: Change `className="...retro"` to `className="...cyphercn-normal"` (stat label)
3. Line 211: Change `className="...retro"` to `className="...cyphercn-normal"` (stat label)
4. Line 229: Change `className="...retro"` to `className="...cyphercn-normal"` (stat label)

Total: 4 changes in character-sheet.tsx
  </action>
  <verify>
- Grep for "retro\|font-retro" in friend-list.tsx - should find 0 matches
- Grep for 'variant="pixel"' in friend-list.tsx - should find 0 matches
- Grep for 'variant="framed"' in friend-list.tsx - should find 1 match
- Grep for "retro" in character-sheet.tsx - should find 0 matches
- Grep for 'variant="framed"' in character-sheet.tsx - should find 1 match
- Run `npx tsc --noEmit` - no errors
  </verify>
  <done>
- friend-list.tsx: Uses cyphercn classes, Avatar variant="framed", no font-retro
- character-sheet.tsx: Uses cyphercn-normal classes, Avatar variant="framed"
- Both components compile without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Update player-profile-card.tsx and leaderboard.tsx</name>
  <files>components/ui/cypher/blocks/player-profile-card.tsx, components/ui/cypher/blocks/leaderboard.tsx</files>
  <action>
**player-profile-card.tsx:**
1. Line 86: Change `variant="pixel"` to `variant="framed"` on Avatar
2. Line 121: Change `className="...retro"` to `className="...cyphercn-normal"` (HP label)
3. Line 138: Change `className="...retro"` to `className="...cyphercn-normal"` (MP label)
4. Line 151: Change `className="...retro"` to `className="...cyphercn-normal"` (XP label)
5. Line 176: Change `className="...retro"` to `className="...cyphercn-normal"` (stat value)

Total: 5 changes in player-profile-card.tsx

**leaderboard.tsx:**
1. Line 153: Change `className="retro text-sm"` to `className="cyphercn text-sm"` (empty message)
2. Line 172: Change `variant="pixel"` to `variant="framed"` on Avatar
3. Line 176: Change `className="retro text-xs"` to `className="cyphercn text-xs"` (fallback)
4. Line 197: Change `"...retro text-xs..."` to `"...cyphercn text-xs..."` (player name)
5. Line 213: Change `"...retro text-xs..."` to `"...cyphercn text-xs..."` (score)
6. Line 234: Change `className={cn("...retro")}` to `className={cn("...cyphercn-normal")}` (pagination)

Total: 6 changes in leaderboard.tsx
  </action>
  <verify>
- Grep for "retro" in player-profile-card.tsx - should find 0 matches
- Grep for 'variant="pixel"' in player-profile-card.tsx - should find 0 matches
- Grep for "retro" in leaderboard.tsx - should find 0 matches
- Grep for 'variant="framed"' in leaderboard.tsx - should find 1 match
- Run `npx tsc --noEmit` - no errors
  </verify>
  <done>
- player-profile-card.tsx: Uses cyphercn-normal classes, Avatar variant="framed"
- leaderboard.tsx: Uses cyphercn classes, Avatar variant="framed"
- Both components compile without errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Update dialogue.tsx Avatar variants</name>
  <files>components/ui/cypher/blocks/dialogue.tsx</files>
  <action>
**dialogue.tsx:**
1. Line 34: Change `variant="pixel"` to `variant="framed"` on first Avatar
2. Line 45: Change `variant="pixel"` to `variant="framed"` on second Avatar

Total: 2 changes in dialogue.tsx
  </action>
  <verify>
- Grep for 'variant="pixel"' in dialogue.tsx - should find 0 matches
- Grep for 'variant="framed"' in dialogue.tsx - should find 2 matches
- Run `npx tsc --noEmit` - no errors
  </verify>
  <done>
- dialogue.tsx: Both Avatar components use variant="framed"
- Component compiles without errors
  </done>
</task>

</tasks>

<verification>
```bash
# Verify no retro/pixel terminology in all five files
grep -rn "retro\|font-retro" components/ui/cypher/blocks/friend-list.tsx components/ui/cypher/blocks/character-sheet.tsx components/ui/cypher/blocks/player-profile-card.tsx components/ui/cypher/blocks/leaderboard.tsx components/ui/cypher/blocks/dialogue.tsx
# Expected: 0 matches

# Verify no variant="pixel" in any file
grep -rn 'variant="pixel"' components/ui/cypher/blocks/friend-list.tsx components/ui/cypher/blocks/character-sheet.tsx components/ui/cypher/blocks/player-profile-card.tsx components/ui/cypher/blocks/leaderboard.tsx components/ui/cypher/blocks/dialogue.tsx
# Expected: 0 matches

# Verify variant="framed" exists
grep -c 'variant="framed"' components/ui/cypher/blocks/friend-list.tsx components/ui/cypher/blocks/character-sheet.tsx components/ui/cypher/blocks/player-profile-card.tsx components/ui/cypher/blocks/leaderboard.tsx components/ui/cypher/blocks/dialogue.tsx
# Expected: 1, 1, 1, 1, 2 respectively

# TypeScript check
npx tsc --noEmit
```
</verification>

<success_criteria>
- friend-list.tsx: cyphercn classes, variant="framed", no font-retro
- character-sheet.tsx: cyphercn-normal classes, variant="framed"
- player-profile-card.tsx: cyphercn-normal classes, variant="framed"
- leaderboard.tsx: cyphercn classes, variant="framed"
- dialogue.tsx: Both Avatars use variant="framed"
- All five components compile without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/02.4-aesthetic-compliance-review/02.4-04-SUMMARY.md`
</output>
