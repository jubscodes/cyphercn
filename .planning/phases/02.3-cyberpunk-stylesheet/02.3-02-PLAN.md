---
phase: 02.3-cyberpunk-stylesheet
plan: 02
type: execute
wave: 2
depends_on: ["02.3-01"]
files_modified:
  - components/ui/cypher/*.tsx
  - components/ui/cypher/blocks/*.tsx
  - components/ui/protheus/*.tsx
autonomous: false

must_haves:
  truths:
    - "All cypher components import cyberpunk.css instead of retro.css"
    - "All protheus components import cyberpunk.css instead of retro.css"
    - "Build completes without CSS import errors"
    - "Components render with phosphor glow and CRT scanline effects visible"
  artifacts:
    - path: "components/ui/cypher/button.tsx"
      contains: "cyberpunk.css"
    - path: "components/ui/protheus/panel.tsx"
      contains: "cyberpunk.css"
    - path: "components/ui/cypher/card.tsx"
      contains: "cyberpunk.css"
  key_links:
    - from: "components/ui/cypher/*.tsx"
      to: "components/ui/cypher/styles/cyberpunk.css"
      via: "import statement"
      pattern: 'import.*cyberpunk\.css'
---

<objective>
Update all component imports from retro.css to cyberpunk.css and verify the cyberpunk aesthetic renders correctly.

Purpose: Wire all components to the new stylesheet and verify the visual transformation is complete.
Output: All 54 components importing cyberpunk.css, build passing, visual verification of effects.
</objective>

<execution_context>
@/Users/jubs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jubs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.3-cyberpunk-stylesheet/02.3-01-SUMMARY.md
@components/ui/cypher/styles/cyberpunk.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all component imports from retro.css to cyberpunk.css</name>
  <files>
    components/ui/cypher/*.tsx
    components/ui/cypher/blocks/*.tsx
    components/ui/protheus/*.tsx
  </files>
  <action>
Find and replace all imports of `retro.css` with `cyberpunk.css` using sed or editor batch replace:

```bash
# Cypher components
find components/ui/cypher -name "*.tsx" -exec sed -i '' 's/retro\.css/cyberpunk.css/g' {} \;

# Protheus components
find components/ui/protheus -name "*.tsx" -exec sed -i '' 's/retro\.css/cyberpunk.css/g' {} \;
```

Or manually update each file:
- Search for: `import "@/components/ui/cypher/styles/retro.css"`
- Replace with: `import "@/components/ui/cypher/styles/cyberpunk.css"`

**Files to update (54 total from grep results):**

Cypher components (48):
- accordion.tsx, alert-dialog.tsx, avatar.tsx, breadcrumb.tsx, button.tsx
- calendar.tsx, card.tsx, checkbox.tsx, collapsible.tsx, command.tsx
- context-menu.tsx, dialog.tsx, drawer.tsx, dropdown-menu.tsx, empty.tsx
- enemy-health-display.tsx, hover-card.tsx, input.tsx, input-otp.tsx
- item.tsx, kbd.tsx, label.tsx, menubar.tsx, navigation-menu.tsx
- pagination.tsx, popover.tsx, progress.tsx, radio-group.tsx, select.tsx
- sheet.tsx, skeleton.tsx, table.tsx, tabs.tsx, textarea.tsx
- toast.tsx, toggle.tsx, toggle-group.tsx, tooltip.tsx
- blocks/character-sheet.tsx, blocks/leaderboard.tsx, blocks/login-form-2.tsx
- blocks/login-form-with-image.tsx, blocks/player-profile-card.tsx
- blocks/quest-log.tsx, blocks/save-slots.tsx, blocks/sidebar.tsx

Protheus components (6):
- alert.tsx, badge.tsx, button.tsx, card.tsx, input.tsx
- panel.tsx, progress.tsx, separator.tsx

Verify no files still reference retro.css after completion.
  </action>
  <verify>
    - No files reference retro.css: `grep -r "retro\.css" components/ui/cypher components/ui/protheus | wc -l` should be 0
    - All component files reference cyberpunk.css: `grep -r "cyberpunk\.css" components/ui/cypher components/ui/protheus | wc -l` should be ~54
  </verify>
  <done>All 54 component files import cyberpunk.css instead of retro.css</done>
</task>

<task type="auto">
  <name>Task 2: Verify build passes</name>
  <files>None (verification only)</files>
  <action>
Run the build to ensure:
1. cyberpunk.css is found by all importing components
2. No CSS parsing errors
3. No broken imports

```bash
npm run build
```

If build fails, check:
- File path spelling: `cyberpunk.css` not `cyberpnk.css`
- All imports use `@/components/ui/cypher/styles/cyberpunk.css`
- No circular imports
  </action>
  <verify>
    - Build completes: `npm run build` exits with code 0
  </verify>
  <done>Build passes with no errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete cyberpunk stylesheet migration:
    - Created cyberpunk.css with typography, phosphor glow, CRT scanlines, theme variants
    - Updated 54 component imports from retro.css to cyberpunk.css
    - Build verified passing
  </what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Visit http://localhost:3000/docs/components/button
    3. Verify phosphor-glow effect on button hover (visible glow around button)
    4. Visit http://localhost:3000/docs/components/card
    5. Verify CRT scanlines visible on cards with scanlines={true} prop
    6. Check that IBM Plex Mono font is applied (not Press Start 2P)
    7. Test theme switching - colors should change but maintain similar brightness

    Expected visual results:
    - Text should be IBM Plex Mono (clean monospace, not chunky 8-bit)
    - Glowing elements should have visible phosphor glow (white base + colored outer)
    - Scanline overlays should be subtle horizontal lines that don't block clicks
    - All 6 theme colors (green, amber, cyan, red, white, orange) should appear similar brightness
  </how-to-verify>
  <resume-signal>Type "approved" if visual effects look correct, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
```bash
# No retro.css imports remain
grep -r "retro\.css" components/ui/cypher components/ui/protheus 2>/dev/null | wc -l

# cyberpunk.css is imported
grep -r "cyberpunk\.css" components/ui/cypher components/ui/protheus 2>/dev/null | wc -l

# Build passes
npm run build
```
</verification>

<success_criteria>
1. Zero files import retro.css in components/ui/cypher, components/ui/protheus
2. 54 files import cyberpunk.css
3. npm run build completes successfully
4. Visual verification confirms phosphor glow and scanlines render correctly
5. IBM Plex Mono typography visible (not Press Start 2P)
</success_criteria>

<output>
After completion, create `.planning/phases/02.3-cyberpunk-stylesheet/02.3-02-SUMMARY.md`
</output>
