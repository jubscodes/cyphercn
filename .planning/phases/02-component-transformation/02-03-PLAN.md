---
phase: 02-component-transformation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui/8bit/table.tsx
  - components/ui/8bit/progress.tsx
  - components/ui/8bit/avatar.tsx
  - components/ui/8bit/tooltip.tsx
  - components/ui/8bit/hover-card.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Table displays data with thin 1px borders and monospace alignment"
    - "Progress bar shows ASCII-style progress indicator"
    - "Avatar displays with thin border and optional glow"
    - "Tooltip appears with thin border and terminal styling"
    - "Hover Card displays with DOS panel styling"
  artifacts:
    - path: "components/ui/8bit/table.tsx"
      provides: "Terminal-style data table with thin borders"
      exports: ["Table", "TableHeader", "TableBody", "TableFooter", "TableRow", "TableHead", "TableCell", "TableCaption"]
    - path: "components/ui/8bit/progress.tsx"
      provides: "ASCII progress bar with block characters"
      exports: ["Progress"]
    - path: "components/ui/8bit/tooltip.tsx"
      provides: "Terminal tooltip with thin border"
      exports: ["Tooltip", "TooltipTrigger", "TooltipContent", "TooltipProvider"]
  key_links:
    - from: "components/ui/8bit/table.tsx"
      to: "components/ui/8bit/styles/protheus.css"
      via: "DOS border utilities"
      pattern: "border-dos|protheus"
    - from: "components/ui/8bit/progress.tsx"
      to: "components/ui/8bit/styles/protheus.css"
      via: "ASCII progress styling"
      pattern: "ascii-progress|protheus"
---

<objective>
Transform 5 data display components (Table, Progress, Avatar, Tooltip, Hover Card) from 8bit pixel aesthetic to Protheus terminal aesthetic.

Purpose: Data display components show information in terminal style. Tables need monospace alignment, progress needs ASCII characters, tooltips need thin borders.

Output: 5 refactored components with terminal data display patterns.
</objective>

<execution_context>
@/Users/jubs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jubs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-transformation/02-RESEARCH.md
@components/ui/8bit/styles/protheus.css
@components/ui/protheus/progress.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Transform Table and Progress components</name>
  <files>
    components/ui/8bit/table.tsx
    components/ui/8bit/progress.tsx
  </files>
  <action>
Transform Table (components/ui/8bit/table.tsx):
1. Remove wrapper div with border-y-6 pattern
2. Remove absolute-positioned border-x-6 overlay div
3. Replace `retro` class with `protheus`
4. Replace import to protheus.css
5. Main Table wrapper: border border-foreground (thin 1px)
6. Keep variant for borderless tables
7. TableHeader: border-b border-foreground (solid, not 4px thick)
8. TableRow: border-b border-foreground/50 (subtle separator, not dashed 4px)
9. TableHead and TableCell: Apply `font-mono` class explicitly for monospace alignment
10. TableCell: Also apply `tabular-nums` class for numeric data alignment (numbers align in columns)
11. Add optional striped variant: odd:bg-foreground/5
12. Ensure all table content inherits monospace font from protheus class

Transform Progress (components/ui/8bit/progress.tsx):
Reference protheus/progress.tsx for pattern:
1. Remove pixel border patterns
2. Replace `retro` with `protheus`
3. Add variant prop: "bar" | "ascii" | "blocks"
4. ASCII variant: Use bracket container and fill characters
   - Empty: [          ] 0%
   - Partial: [####      ] 40%
   - Full: [##########] 100%
   - Use # or block characters for fill
5. Bar variant: thin border container with filled inner div
6. Blocks variant: series of block characters
7. Add `showPercent?: boolean` prop to display percentage text
8. Container: protheus font, optional phosphor-glow
9. Keep aria-valuenow, aria-valuemin, aria-valuemax for accessibility
  </action>
  <verify>
Run: `npm run lint -- --quiet components/ui/8bit/table.tsx components/ui/8bit/progress.tsx`
Manual: Table shows thin 1px borders
Manual: Create a test table with mixed data (names, numbers, dates) and verify columns align vertically - numbers should stack evenly, text columns should have consistent character width
Manual: Inspect TableCell element and confirm `font-mono` and `tabular-nums` classes are present
Manual: Progress shows ASCII [####------] style indicator
  </verify>
  <done>
Table displays with thin terminal borders and explicit monospace alignment via font-mono and tabular-nums classes. Progress shows ASCII-style indicator with bracket container.
  </done>
</task>

<task type="auto">
  <name>Task 2: Transform Avatar, Tooltip, Hover Card components</name>
  <files>
    components/ui/8bit/avatar.tsx
    components/ui/8bit/tooltip.tsx
    components/ui/8bit/hover-card.tsx
  </files>
  <action>
Transform Avatar (components/ui/8bit/avatar.tsx):
1. Remove complex pixel border pattern (the 8bit avatar has extensive corner blocks)
2. Replace `retro` with `protheus`
3. Simple styling: border border-foreground, square or slightly rounded
4. Add `glow?: boolean` prop for phosphor-border-glow
5. Fallback: uppercase initials with protheus font
6. Keep Radix AvatarPrimitive for image loading states
7. Size variants: sm (h-8 w-8), default (h-10 w-10), lg (h-12 w-12)

Transform Tooltip (components/ui/8bit/tooltip.tsx):
1. Remove pixel border patterns from TooltipContent
2. Replace `retro` with `protheus`
3. TooltipContent: border border-foreground bg-popover p-2 text-xs
4. Add subtle phosphor-glow on content (text-shadow)
5. Arrow styling: match border color (if using arrow)
6. Keep Radix TooltipPrimitive for positioning and accessibility
7. Add entry animation (fade in, respecting prefers-reduced-motion)

Transform Hover Card (components/ui/8bit/hover-card.tsx):
1. Remove pixel border patterns from HoverCardContent
2. Replace `retro` with `protheus`
3. HoverCardContent: border border-foreground bg-popover p-4
4. Style as DOS panel: can add optional title with dos-panel-title
5. Add phosphor-border-glow option
6. Keep Radix HoverCardPrimitive for accessibility
7. Add entry animation (scale/fade, respecting prefers-reduced-motion)
  </action>
  <verify>
Run: `npm run lint -- --quiet components/ui/8bit/avatar.tsx components/ui/8bit/tooltip.tsx components/ui/8bit/hover-card.tsx`
Manual: Avatar shows thin border, fallback displays monospace initials
Manual: Tooltip appears with thin border and terminal font
  </verify>
  <done>
Avatar, Tooltip, and Hover Card display terminal aesthetic with thin borders and appropriate glow effects
  </done>
</task>

</tasks>

<verification>
1. Run linter on all 5 modified files
2. Visual check: Table has thin 1px borders, no 4px or 6px borders
3. Visual check: Table columns align properly - load sample data with varying content lengths and verify vertical alignment
4. Visual check: Progress bar shows [####------] ASCII style
5. Visual check: Avatar has thin square border
6. Visual check: Tooltip and Hover Card appear with thin borders
7. Theme test: All components follow theme color via currentColor
8. Accessibility: Progress has correct ARIA attributes
</verification>

<success_criteria>
- Table displays data with thin 1px terminal borders
- Table text aligns in monospace columns via explicit font-mono and tabular-nums classes
- Progress shows ASCII-style indicator with brackets and fill characters
- Avatar displays with thin border and optional glow
- Tooltip and Hover Card have thin borders and terminal styling
- Linter passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-transformation/02-03-SUMMARY.md`
</output>
