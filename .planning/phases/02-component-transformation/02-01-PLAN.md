---
phase: 02-component-transformation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui/8bit/button.tsx
  - components/ui/8bit/input.tsx
  - components/ui/8bit/textarea.tsx
  - components/ui/8bit/checkbox.tsx
  - components/ui/8bit/radio-group.tsx
  - components/ui/8bit/switch.tsx
  - components/ui/8bit/toggle.tsx
  - components/ui/8bit/toggle-group.tsx
  - components/ui/8bit/label.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "All 9 core interactive components display thin 1px terminal borders"
    - "Components use IBM Plex Mono font via protheus class"
    - "Interactive components have visible glow on hover/focus"
    - "Focus states are keyboard-visible and WCAG AA compliant"
    - "Components work correctly in all 6 monochrome themes"
  artifacts:
    - path: "components/ui/8bit/button.tsx"
      provides: "Terminal-style button with glow prop"
      exports: ["Button", "buttonVariants"]
    - path: "components/ui/8bit/input.tsx"
      provides: "Terminal input with prompt prefix"
      exports: ["Input"]
    - path: "components/ui/8bit/checkbox.tsx"
      provides: "Terminal checkbox with bracket indicator"
      exports: ["Checkbox"]
  key_links:
    - from: "components/ui/8bit/button.tsx"
      to: "components/ui/8bit/styles/protheus.css"
      via: "CSS import and utility classes"
      pattern: "phosphor-.*glow|protheus"
    - from: "components/ui/8bit/input.tsx"
      to: "components/ui/8bit/styles/protheus.css"
      via: "Terminal prompt styling"
      pattern: "terminal-cursor|protheus"
---

<objective>
Transform 9 core interactive components (Button, Input, Textarea, Checkbox, Radio, Switch, Toggle, Toggle Group, Label) from 8bit pixel aesthetic to Protheus terminal aesthetic.

Purpose: These foundational components are used by most other components. Transforming them first establishes patterns for the rest of Phase 2.

Output: 9 refactored components with thin 1px borders, phosphor glow effects, IBM Plex Mono font, and WCAG-compliant focus states.
</objective>

<execution_context>
@/Users/jubs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jubs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-transformation/02-RESEARCH.md
@components/ui/8bit/styles/protheus.css
@components/ui/protheus/button.tsx
@components/ui/protheus/input.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Transform Button and Input components</name>
  <files>
    components/ui/8bit/button.tsx
    components/ui/8bit/input.tsx
    components/ui/8bit/textarea.tsx
  </files>
  <action>
Transform Button (components/ui/8bit/button.tsx):
1. Remove ALL absolute-positioned border divs (the 12+ divs creating pixel corners)
2. Remove wrapper div pattern for pixel borders
3. Replace `retro` class with `protheus`
4. Replace `@/components/ui/8bit/styles/retro.css` import with `@/components/ui/8bit/styles/protheus.css`
5. Add `glow?: boolean` prop to interface
6. Use cva variants pattern from protheus/button.tsx:
   - default: "bg-primary text-primary-foreground border border-primary hover:bg-primary/90 phosphor-border-glow"
   - outline: "border border-foreground bg-transparent text-foreground hover:bg-foreground/10"
   - ghost: "bg-transparent text-foreground hover:bg-foreground/10"
   - terminal: "bg-transparent text-foreground border-0 hover:text-primary before:content-['['] after:content-[']']"
   - destructive: "bg-destructive text-white border border-destructive hover:bg-destructive/90"
7. Add focus-visible styling: use focus-glow class for terminal glow focus
8. Use Slot from @radix-ui/react-slot for asChild pattern (simpler than current approach)

Transform Input (components/ui/8bit/input.tsx):
1. Remove wrapper div with border-y-6 pattern
2. Remove absolute-positioned border-x-6 overlay div
3. Replace `retro` class with `protheus` or `protheus-normal`
4. Add `prompt?: string` prop (default ">") for terminal prompt prefix
5. Add `glow?: boolean` prop
6. Structure: flex container with prompt span + input + optional terminal-cursor span
7. Input itself: bg-transparent, border-0, outline-none
8. Container: optionally border border-foreground for boxed variant

Transform Textarea (components/ui/8bit/textarea.tsx):
1. Same pattern as Input - remove pixel borders
2. Add `glow?: boolean` prop
3. Use protheus-normal class (normal case for text areas)
4. Add thin 1px border: border border-foreground
  </action>
  <verify>
Run: `npm run lint -- --quiet components/ui/8bit/button.tsx components/ui/8bit/input.tsx components/ui/8bit/textarea.tsx`
Manual: Components render with thin 1px borders, no thick 6px pixel borders visible
  </verify>
  <done>
Button, Input, and Textarea display thin terminal borders with optional phosphor glow, using IBM Plex Mono font
  </done>
</task>

<task type="auto">
  <name>Task 2: Transform form control components</name>
  <files>
    components/ui/8bit/checkbox.tsx
    components/ui/8bit/radio-group.tsx
    components/ui/8bit/switch.tsx
    components/ui/8bit/toggle.tsx
    components/ui/8bit/toggle-group.tsx
    components/ui/8bit/label.tsx
  </files>
  <action>
Transform Checkbox (components/ui/8bit/checkbox.tsx):
1. Remove pixel border divs/wrappers
2. Replace `retro` with `protheus`
3. Replace import to protheus.css
4. Style as terminal checkbox: border border-foreground, bg-transparent
5. Checked state: use "[X]" or filled indicator with phosphor-glow
6. Add focus-visible: focus-glow class
7. Keep Radix CheckboxPrimitive for accessibility

Transform Radio Group (components/ui/8bit/radio-group.tsx):
1. Remove pixel borders from RadioGroupItem
2. Replace `retro` with `protheus`
3. Style as terminal radio: border border-foreground rounded-full
4. Selected state: filled circle with phosphor-glow
5. Add focus-glow on focus-visible

Transform Switch (components/ui/8bit/switch.tsx):
1. Remove pixel border patterns
2. Replace `retro` with `protheus`
3. Style as terminal toggle: border border-foreground, slim track
4. Thumb: small square or circle, position changes on state
5. ON state: phosphor-glow on track
6. Add focus-glow on focus-visible

Transform Toggle (components/ui/8bit/toggle.tsx):
1. Remove pixel borders
2. Replace `retro` with `protheus`
3. Style with thin border, pressed state shows phosphor-border-glow
4. Add focus-visible styling

Transform Toggle Group (components/ui/8bit/toggle-group.tsx):
1. Remove pixel borders
2. Replace `retro` with `protheus`
3. Container: inline-flex, items touch (no gap or negative margin)
4. Items: share borders (use -ml-px to overlap)
5. Active item: bg-foreground text-background or phosphor-border-glow
6. Keep Radix ToggleGroupPrimitive for accessibility

Transform Label (components/ui/8bit/label.tsx):
1. Simple change: replace `retro` with `protheus`
2. Add uppercase tracking-wider for terminal aesthetic
3. Keep Radix LabelPrimitive for accessibility
  </action>
  <verify>
Run: `npm run lint -- --quiet components/ui/8bit/checkbox.tsx components/ui/8bit/radio-group.tsx components/ui/8bit/switch.tsx components/ui/8bit/toggle.tsx components/ui/8bit/toggle-group.tsx components/ui/8bit/label.tsx`
Manual: Form controls show thin borders, keyboard focus is visible with glow effect
  </verify>
  <done>
All 6 form control components display terminal aesthetic with 1px borders, phosphor glow on interaction, and WCAG-compliant focus states
  </done>
</task>

</tasks>

<verification>
1. Run linter on all 9 modified files: `npm run lint -- --quiet components/ui/8bit/button.tsx components/ui/8bit/input.tsx components/ui/8bit/textarea.tsx components/ui/8bit/checkbox.tsx components/ui/8bit/radio-group.tsx components/ui/8bit/switch.tsx components/ui/8bit/toggle.tsx components/ui/8bit/toggle-group.tsx components/ui/8bit/label.tsx`
2. Visual check: No 6px pixel borders visible on any component
3. Visual check: 1px thin borders visible with currentColor
4. Visual check: Phosphor glow appears on hover/focus
5. Keyboard test: Tab through components, focus ring visible
6. Theme test: Switch between green, amber, cyan themes - borders follow theme color
</verification>

<success_criteria>
- All 9 core interactive components use thin 1px borders (no pixel blocks)
- Components import protheus.css instead of retro.css
- Components use `protheus` class instead of `retro`
- Interactive components have `glow` prop
- Focus states use focus-glow or similar WCAG-compliant indicator
- Linter passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-transformation/02-01-SUMMARY.md`
</output>
