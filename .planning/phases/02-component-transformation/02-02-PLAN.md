---
phase: 02-component-transformation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui/8bit/card.tsx
  - components/ui/8bit/alert.tsx
  - components/ui/8bit/badge.tsx
  - components/ui/8bit/separator.tsx
  - components/ui/8bit/skeleton.tsx
  - components/ui/8bit/accordion.tsx
  - components/ui/8bit/collapsible.tsx
  - components/ui/8bit/kbd.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "All 8 container components display thin 1px terminal borders"
    - "Card component supports optional title in border-break style"
    - "Alert component shows level prefixes [INFO], [WARN], [ERROR]"
    - "Badge component has bracket variant for terminal aesthetic"
    - "Separator uses thin 1px line with optional dashed style"
    - "Kbd component displays keyboard shortcuts with terminal styling"
  artifacts:
    - path: "components/ui/8bit/card.tsx"
      provides: "Terminal card with optional DOS panel title"
      exports: ["Card", "CardHeader", "CardTitle", "CardDescription", "CardContent", "CardFooter"]
    - path: "components/ui/8bit/alert.tsx"
      provides: "Alert with terminal level prefixes"
      exports: ["Alert", "AlertTitle", "AlertDescription"]
    - path: "components/ui/8bit/badge.tsx"
      provides: "Badge with bracket/tag variants"
      exports: ["Badge", "badgeVariants"]
    - path: "components/ui/8bit/kbd.tsx"
      provides: "Keyboard shortcut display"
      exports: ["Kbd"]
  key_links:
    - from: "components/ui/8bit/card.tsx"
      to: "components/ui/8bit/styles/protheus.css"
      via: "DOS panel utilities"
      pattern: "dos-panel|border-dos"
    - from: "components/ui/8bit/alert.tsx"
      to: "components/ui/8bit/styles/protheus.css"
      via: "Phosphor glow and border utilities"
      pattern: "phosphor-border-glow|border-dos"
---

<objective>
Transform 8 container and layout components (Card, Alert, Badge, Separator, Skeleton, Accordion, Collapsible, Kbd) from 8bit pixel aesthetic to Protheus terminal aesthetic.

Purpose: Container components define the visual structure of the UI. Terminal-style containers with thin borders and optional glow create the command center aesthetic.

Output: 8 refactored components with DOS-style borders, terminal prefixes, and bracket styling.
</objective>

<execution_context>
@/Users/jubs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jubs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-transformation/02-RESEARCH.md
@components/ui/8bit/styles/protheus.css
@components/ui/protheus/card.tsx
@components/ui/protheus/alert.tsx
@components/ui/protheus/badge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Transform Card and Alert components</name>
  <files>
    components/ui/8bit/card.tsx
    components/ui/8bit/alert.tsx
  </files>
  <action>
Transform Card (components/ui/8bit/card.tsx):
1. Remove wrapper div with border-y-6 pattern
2. Remove absolute-positioned border-x-6 overlay div
3. Replace `retro` class with `protheus`
4. Replace import to protheus.css
5. Add `title?: string` prop to Card for DOS panel title (uses dos-panel-title utility)
6. Add `glow?: boolean` prop
7. Add `scanlines?: boolean` prop for optional CRT effect
8. Main Card: border border-foreground bg-background p-4
9. With title: use dos-panel class or data-title attribute for border-break title
10. CardHeader: flex flex-col gap-1.5 pb-3 mb-3 border-b border-foreground/30
11. CardTitle: text-sm font-semibold tracking-wider uppercase phosphor-glow
12. CardDescription: protheus-normal text-xs text-muted-foreground
13. CardContent: protheus-normal text-sm
14. CardFooter: flex items-center gap-2 pt-3 mt-3 border-t border-foreground/30

Transform Alert (components/ui/8bit/alert.tsx):
1. Remove pixel border patterns
2. Replace `retro` with `protheus`
3. Add level prefix constants:
   - default: "[INFO]"
   - warning: "[WARN]"
   - error: "[ERROR]"
   - success: "[OK]"
4. Add `glow?: boolean` prop
5. Structure: border border-foreground p-3, with flex layout
6. Prefix span with font-bold shrink-0
7. Content in flex-1 with protheus-normal
8. Variant styling via color adjustments (text-destructive for error, etc.)
9. Add phosphor-border-glow when glow={true}
  </action>
  <verify>
Run: `npm run lint -- --quiet components/ui/8bit/card.tsx components/ui/8bit/alert.tsx`
Manual: Card shows thin 1px border, optional title appears in border-break style
Manual: Alert shows [INFO], [WARN] etc. prefixes with thin border
  </verify>
  <done>
Card and Alert components display terminal aesthetic with DOS panel styling and level prefixes
  </done>
</task>

<task type="auto">
  <name>Task 2: Transform Badge, Separator, Skeleton, Accordion, Collapsible, Kbd</name>
  <files>
    components/ui/8bit/badge.tsx
    components/ui/8bit/separator.tsx
    components/ui/8bit/skeleton.tsx
    components/ui/8bit/accordion.tsx
    components/ui/8bit/collapsible.tsx
    components/ui/8bit/kbd.tsx
  </files>
  <action>
Transform Badge (components/ui/8bit/badge.tsx):
1. Remove pixel border patterns
2. Replace `retro` with `protheus`
3. Use cva variants from protheus/badge.tsx:
   - default: "border border-foreground px-2 py-0.5"
   - bracket: "before:content-['['] after:content-[']'] px-1" (no border)
   - tag: "before:content-['<'] after:content-['>'] px-0.5 text-foreground/70"
   - dot: "gap-1.5" (with status dot indicator)
4. Base: inline-flex items-center text-xs uppercase tracking-wider

Transform Separator (components/ui/8bit/separator.tsx):
1. Remove dashed pixel pattern
2. Replace `retro` with `protheus`
3. Horizontal: border-t border-foreground (thin 1px line)
4. Vertical: border-l border-foreground
5. Add variant for dashed: border-dashed
6. Add variant for double: use dos-hr-double (3px double)
7. Optional: use box-drawing characters for ASCII separator

Transform Skeleton (components/ui/8bit/skeleton.tsx):
1. Remove pixel border patterns
2. Replace `retro` with `protheus`
3. Use dither pattern for loading state: dither-halftone or dither-lines-h
4. Subtle background animation (respects prefers-reduced-motion)
5. Thin border optional: border border-foreground/20

Transform Accordion (components/ui/8bit/accordion.tsx):
1. Remove pixel borders from trigger and content
2. Replace `retro` with `protheus`
3. Trigger: border-b border-foreground, hover shows phosphor-glow on text
4. Add bracket indicator for expanded state: [+] / [-] or [>] / [v]
5. Content: border-b border-foreground/30
6. Keep Radix primitives for accessibility

Transform Collapsible (components/ui/8bit/collapsible.tsx):
1. Remove pixel border patterns
2. Replace `retro` with `protheus`
3. Trigger: inline, hover shows phosphor-glow
4. Content: optional border border-foreground/30
5. Keep Radix primitives for accessibility

Transform Kbd (components/ui/8bit/kbd.tsx):
1. Remove pixel border patterns
2. Replace `retro` with `protheus`
3. Style as terminal key indicator:
   - border border-foreground px-1.5 py-0.5
   - protheus uppercase text-xs
   - bg-foreground/10 for subtle key background
4. Multiple keys: separate with + character
5. Optional: use [KEY] bracket styling instead of border
  </action>
  <verify>
Run: `npm run lint -- --quiet components/ui/8bit/badge.tsx components/ui/8bit/separator.tsx components/ui/8bit/skeleton.tsx components/ui/8bit/accordion.tsx components/ui/8bit/collapsible.tsx components/ui/8bit/kbd.tsx`
Manual: Badge with bracket variant shows [text] styling
Manual: Separator is thin 1px line, not thick dashed pixels
Manual: Kbd shows keyboard shortcut with terminal styling
  </verify>
  <done>
All 6 components (Badge, Separator, Skeleton, Accordion, Collapsible, Kbd) display terminal aesthetic with thin borders and bracket styling
  </done>
</task>

</tasks>

<verification>
1. Run linter on all 8 modified files
2. Visual check: No 6px pixel borders visible on any component
3. Visual check: Card title appears in border-break style (interrupts border line)
4. Visual check: Alert shows terminal prefix like [INFO] before content
5. Visual check: Badge bracket variant displays [text] without border
6. Visual check: Separator is thin 1px line
7. Visual check: Kbd shows keyboard shortcuts with terminal styling
8. Theme test: Switch themes - all borders follow theme color via currentColor
</verification>

<success_criteria>
- All 8 container components use thin 1px borders
- Components import protheus.css instead of retro.css
- Card supports title prop for DOS panel style
- Alert shows level prefixes [INFO], [WARN], [ERROR], [OK]
- Badge has bracket and tag variants
- Separator uses thin 1px line
- Kbd displays keyboard shortcuts with terminal styling
- Linter passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-transformation/02-02-SUMMARY.md`
</output>
