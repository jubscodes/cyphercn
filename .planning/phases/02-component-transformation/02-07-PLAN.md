---
phase: 02-component-transformation
plan: 07
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - components/ui/8bit/command.tsx
  - components/ui/8bit/resizable.tsx
  - components/ui/8bit/carousel.tsx
  - components/ui/8bit/scroll-area.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Command Palette displays with thin borders and terminal prompt styling"
    - "Command input shows > prompt and terminal cursor"
    - "Command items use bracket styling for shortcuts"
    - "Resizable panels have thin borders with terminal drag handle"
    - "Carousel shows navigation with < > terminal arrows"
    - "Scroll Area has thin terminal-style scrollbar"
  artifacts:
    - path: "components/ui/8bit/command.tsx"
      provides: "Terminal command palette with prompt styling"
      exports: ["Command", "CommandDialog", "CommandInput", "CommandList", "CommandEmpty", "CommandGroup", "CommandItem", "CommandShortcut", "CommandSeparator"]
    - path: "components/ui/8bit/resizable.tsx"
      provides: "Resizable panels with thin borders"
      exports: ["ResizablePanelGroup", "ResizablePanel", "ResizableHandle"]
    - path: "components/ui/8bit/carousel.tsx"
      provides: "Terminal carousel with ASCII navigation"
      exports: ["Carousel", "CarouselContent", "CarouselItem", "CarouselPrevious", "CarouselNext"]
  key_links:
    - from: "components/ui/8bit/command.tsx"
      to: "components/ui/8bit/styles/protheus.css"
      via: "Terminal prompt and cursor styling"
      pattern: "terminal-.*|protheus|phosphor-glow"
    - from: "components/ui/8bit/scroll-area.tsx"
      to: "components/ui/8bit/styles/protheus.css"
      via: "Terminal scrollbar styling"
      pattern: "protheus"
---

<objective>
Transform 4 complex/specialized components (Command, Resizable, Carousel, Scroll Area) from 8bit pixel aesthetic to Protheus terminal aesthetic.

Purpose: These are the most complex components. Command Palette is especially important as it's a signature terminal UI element. The transformation must maintain all functionality while achieving terminal aesthetic.

Output: 4 refactored complex components with terminal command-line styling.
</objective>

<execution_context>
@/Users/jubs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jubs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-transformation/02-RESEARCH.md
@components/ui/8bit/styles/protheus.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Transform Command component (Command Palette)</name>
  <files>
    components/ui/8bit/command.tsx
  </files>
  <action>
Transform Command (components/ui/8bit/command.tsx):
This is a critical component for terminal aesthetic.

1. Remove ALL pixel border divs (12+ absolute-positioned corner blocks)
2. Replace `retro` with `protheus`
3. Replace import to protheus.css
4. Command container:
   - border border-foreground bg-background
   - Add optional dos-panel or dos-window styling
   - phosphor-border-glow
5. CommandInput wrapper:
   - border-b border-foreground
   - Add terminal prompt: > prefix (use terminal-prompt class)
   - Add terminal cursor after input (terminal-cursor class)
   - Remove the pixel art search icon, use simple text > or SEARCH: prefix
6. CommandInput field:
   - bg-transparent border-0 outline-none
   - protheus font
   - placeholder: "Type a command..."
7. CommandList:
   - protheus-normal for content
   - Thin scrollbar styling
8. CommandEmpty:
   - Show "No results found." in terminal style
   - Optional: "> Command not recognized"
9. CommandGroup:
   - Group heading: protheus uppercase text-xs text-muted-foreground
   - Add optional [GROUP] bracket prefix
10. CommandItem:
   - px-3 py-2 protheus-normal
   - hover: bg-foreground/10 phosphor-glow on text
   - focus/selected: bg-foreground text-background
   - Remove dashed pixel borders
11. CommandShortcut:
   - Use bracket style: [Ctrl+K] instead of just Ctrl+K
   - text-xs text-muted-foreground
12. CommandSeparator:
   - border-t border-foreground/30 (thin line)
   - Remove complex dashed pattern
13. CommandDialog:
   - Use Dialog with terminal styling
   - Header can show "COMMAND PALETTE" title
14. Keep cmdk functionality for fuzzy search
  </action>
  <verify>
Run: `npm run lint -- --quiet components/ui/8bit/command.tsx`
Manual: Command palette shows > prompt in input field
Manual: No pixel borders visible, only thin 1px borders
Manual: Shortcuts display as [Ctrl+K] with brackets
Manual: Search still works, items filter correctly
  </verify>
  <done>
Command Palette displays terminal-style prompt with thin borders, bracket shortcuts, and phosphor glow
  </done>
</task>

<task type="auto">
  <name>Task 2: Transform Resizable, Carousel, Scroll Area components</name>
  <files>
    components/ui/8bit/resizable.tsx
    components/ui/8bit/carousel.tsx
    components/ui/8bit/scroll-area.tsx
  </files>
  <action>
Transform Resizable (components/ui/8bit/resizable.tsx):
1. Remove pixel border patterns
2. Replace `retro` with `protheus`
3. ResizablePanelGroup:
   - Container with optional border border-foreground
4. ResizablePanel:
   - Individual panels, can have thin borders
5. ResizableHandle:
   - Thin line separator: border-l or border-t (1px)
   - Hover: phosphor-glow effect
   - Optional: show grip pattern with || or == characters
   - Cursor: col-resize or row-resize
   - Focus: focus-glow
6. Keep react-resizable-panels functionality

Transform Carousel (components/ui/8bit/carousel.tsx):
1. Remove pixel border patterns
2. Replace `retro` with `protheus`
3. Carousel container: relative, optional border
4. CarouselContent: flex overflow-hidden
5. CarouselItem: protheus, min-w-0 shrink-0
6. CarouselPrevious:
   - Use < character or [<] or [PREV]
   - border border-foreground p-2
   - hover: phosphor-border-glow
   - Position: left side, vertically centered
7. CarouselNext:
   - Use > character or [>] or [NEXT]
   - Same styling as Previous
   - Position: right side, vertically centered
8. Optional: Add dots indicator with terminal style [ o o o ]
9. Keep Embla Carousel functionality

Transform Scroll Area (components/ui/8bit/scroll-area.tsx):
1. Remove pixel border patterns
2. Replace `retro` with `protheus`
3. ScrollArea container: relative overflow-hidden
4. Scrollbar styling:
   - Track: bg-foreground/10 (subtle)
   - Thumb: bg-foreground, thin (w-2 for vertical, h-2 for horizontal)
   - Hover on thumb: phosphor-glow
   - Optional: ASCII scrollbar characters
5. ScrollAreaViewport: standard viewport
6. Keep Radix Scroll Area primitives
  </action>
  <verify>
Run: `npm run lint -- --quiet components/ui/8bit/resizable.tsx components/ui/8bit/carousel.tsx components/ui/8bit/scroll-area.tsx`
Manual: Resizable handle is thin line, shows glow on hover
Manual: Carousel prev/next use < > characters with thin borders
Manual: Scroll area has thin scrollbar
  </verify>
  <done>
Resizable, Carousel, and Scroll Area display terminal aesthetic with thin borders and appropriate indicators
  </done>
</task>

</tasks>

<verification>
1. Run linter on all 4 modified files
2. Visual check: Command palette has thin borders, shows > prompt
3. Visual check: Command shortcuts show as [Ctrl+K] with brackets
4. Visual check: Resizable handle is thin line
5. Visual check: Carousel uses < > for navigation
6. Visual check: Scroll area has thin scrollbar
7. Functional test: Command search filters items correctly
8. Functional test: Resizable panels can be dragged
9. Functional test: Carousel can navigate between items
10. Theme test: All borders follow theme color
</verification>

<success_criteria>
- Command Palette uses thin 1px borders with terminal prompt styling
- Command input shows > prompt and optional terminal cursor
- Shortcuts display with [bracket] styling
- Resizable panels have thin border handles
- Carousel navigation uses < > characters
- Scroll Area has thin terminal-style scrollbar
- All functionality preserved (search, resize, scroll, navigate)
- Linter passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-transformation/02-07-SUMMARY.md`
</output>
