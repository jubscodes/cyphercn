---
phase: 02-component-transformation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui/8bit/dialog.tsx
  - components/ui/8bit/alert-dialog.tsx
  - components/ui/8bit/sheet.tsx
  - components/ui/8bit/drawer.tsx
  - components/ui/8bit/popover.tsx
  - components/ui/8bit/toast.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Dialog displays as DOS window with titlebar and thin borders"
    - "Alert Dialog shows confirmation with terminal styling and level prefix"
    - "Sheet slides in with thin border and optional scanlines"
    - "Popover appears with thin border and terminal styling"
    - "Toast notifications display with level prefix and thin border"
  artifacts:
    - path: "components/ui/8bit/dialog.tsx"
      provides: "Terminal dialog with DOS window styling"
      exports: ["Dialog", "DialogTrigger", "DialogContent", "DialogHeader", "DialogFooter", "DialogTitle", "DialogDescription", "DialogClose"]
    - path: "components/ui/8bit/sheet.tsx"
      provides: "Terminal side panel with thin borders"
      exports: ["Sheet", "SheetTrigger", "SheetContent", "SheetHeader", "SheetFooter", "SheetTitle", "SheetDescription", "SheetClose"]
    - path: "components/ui/8bit/toast.tsx"
      provides: "Terminal toast with level prefixes"
      exports: ["Toast", "ToastAction", "ToastClose", "ToastDescription", "ToastProvider", "ToastTitle", "ToastViewport"]
  key_links:
    - from: "components/ui/8bit/dialog.tsx"
      to: "components/ui/8bit/styles/protheus.css"
      via: "DOS window utilities"
      pattern: "dos-window|border-dos"
    - from: "components/ui/8bit/toast.tsx"
      to: "components/ui/8bit/styles/protheus.css"
      via: "Phosphor glow and terminal styling"
      pattern: "phosphor-.*glow|protheus"
---

<objective>
Transform 6 overlay and modal components (Dialog, Alert Dialog, Sheet, Drawer, Popover, Toast) from 8bit pixel aesthetic to Protheus terminal aesthetic.

Purpose: Overlays are prominent UI elements. They should feel like terminal windows or system messages with DOS-style titlebar and thin borders.

Output: 6 refactored overlay components with DOS window styling and terminal message patterns.
</objective>

<execution_context>
@/Users/jubs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jubs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-transformation/02-RESEARCH.md
@components/ui/8bit/styles/protheus.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Transform Dialog and Alert Dialog components</name>
  <files>
    components/ui/8bit/dialog.tsx
    components/ui/8bit/alert-dialog.tsx
  </files>
  <action>
Transform Dialog (components/ui/8bit/dialog.tsx):
1. Remove ALL absolute-positioned border divs (border-x-6, border-y-6)
2. Replace `retro` class with `protheus`
3. Replace import to protheus.css
4. DialogContent structure as DOS window:
   - Outer: border border-foreground bg-background
   - Optional: dos-window class for titlebar variant
   - Add `variant?: "default" | "window"` prop
5. Window variant structure:
   - dos-window-titlebar: flex items-center justify-between, border-b, bg-primary
   - Title text uppercase tracking-wider
   - Close button in titlebar (dos-window-btn style)
   - dos-window-content: main content area with padding
6. Default variant: simple bordered modal
7. DialogHeader: border-b border-foreground/30 if using default variant
8. DialogTitle: protheus uppercase tracking-wider, optional phosphor-glow
9. DialogDescription: protheus-normal text-sm text-muted-foreground
10. DialogFooter: flex gap-2 pt-4, border-t border-foreground/30
11. Keep Radix Dialog primitives for accessibility and focus trap

Transform Alert Dialog (components/ui/8bit/alert-dialog.tsx):
1. Remove pixel border patterns
2. Replace `retro` with `protheus`
3. Add variant for alert level: "warning" | "error" | "info"
4. AlertDialogContent: border border-foreground (or variant border color)
5. Add level prefix to title area: [WARNING], [ERROR], [CONFIRM]
6. AlertDialogTitle: protheus uppercase with prefix
7. AlertDialogDescription: protheus-normal
8. AlertDialogAction/Cancel: use terminal button styling
9. Keep Radix AlertDialog primitives for accessibility
  </action>
  <verify>
Run: `npm run lint -- --quiet components/ui/8bit/dialog.tsx components/ui/8bit/alert-dialog.tsx`
Manual: Dialog with window variant shows titlebar with close button
Manual: Alert Dialog shows [WARNING] or similar prefix
  </verify>
  <done>
Dialog and Alert Dialog display as DOS windows with titlebar option and terminal styling
  </done>
</task>

<task type="auto">
  <name>Task 2: Transform Sheet, Drawer, Popover, Toast components</name>
  <files>
    components/ui/8bit/sheet.tsx
    components/ui/8bit/drawer.tsx
    components/ui/8bit/popover.tsx
    components/ui/8bit/toast.tsx
  </files>
  <action>
Transform Sheet (components/ui/8bit/sheet.tsx):
1. Remove pixel border patterns
2. Replace `retro` with `protheus`
3. SheetContent: border border-foreground (on visible edge)
   - side="right": border-l only
   - side="left": border-r only
   - side="top": border-b only
   - side="bottom": border-t only
4. Add `scanlines?: boolean` prop for CRT effect (crt-scanlines-subtle)
5. SheetHeader: optional border-b border-foreground/30
6. SheetTitle: protheus uppercase phosphor-glow
7. Keep slide animation (respects prefers-reduced-motion)
8. Keep Radix Sheet primitives

Transform Drawer (components/ui/8bit/drawer.tsx):
1. Remove pixel border patterns
2. Replace `retro` with `protheus`
3. Similar to Sheet but typically bottom-only
4. DrawerContent: border-t border-foreground
5. Handle indicator: thin line instead of thick bar
6. DrawerHeader/Title/Description: terminal styling
7. Keep Vaul drawer functionality

Transform Popover (components/ui/8bit/popover.tsx):
1. Remove pixel border patterns
2. Replace `retro` with `protheus`
3. PopoverContent: border border-foreground bg-popover p-3
4. Add optional phosphor-border-glow
5. Arrow: match border color if using arrow
6. Keep Radix Popover primitives for positioning

Transform Toast (components/ui/8bit/toast.tsx):
1. Remove pixel border patterns
2. Replace `retro` with `protheus`
3. Add level prefixes based on variant:
   - default: "[MSG]"
   - success: "[OK]"
   - error: "[ERR]"
   - warning: "[WARN]"
4. Toast container: border border-foreground bg-background
5. Add phosphor-border-glow on entry
6. ToastTitle: protheus uppercase
7. ToastDescription: protheus-normal text-sm
8. ToastAction: terminal button styling
9. Entry/exit animations (respects prefers-reduced-motion)
10. Keep Radix Toast primitives
  </action>
  <verify>
Run: `npm run lint -- --quiet components/ui/8bit/sheet.tsx components/ui/8bit/drawer.tsx components/ui/8bit/popover.tsx components/ui/8bit/toast.tsx`
Manual: Sheet slides in with thin border on visible edge
Manual: Toast shows [OK], [ERR] prefix based on variant
  </verify>
  <done>
Sheet, Drawer, Popover, and Toast display terminal aesthetic with thin borders and appropriate prefixes
  </done>
</task>

</tasks>

<verification>
1. Run linter on all 6 modified files
2. Visual check: Dialog with window variant shows DOS-style titlebar
3. Visual check: Alert Dialog shows level prefix
4. Visual check: Sheet has thin border on visible edge only
5. Visual check: Toast notifications show prefix like [OK], [ERR]
6. Visual check: Popover has thin border
7. Accessibility: Focus trap works in Dialog, keyboard dismiss works
8. Theme test: Dialog displays correctly in green, amber, cyan, red, white, and orange monochrome themes with titlebar styling following theme color
</verification>

<success_criteria>
- All 6 overlay components use thin 1px borders
- Dialog supports window variant with titlebar
- Alert Dialog shows level prefix
- Sheet/Drawer have border on visible edge
- Toast shows level prefix based on variant
- All overlays maintain accessibility (focus trap, keyboard nav)
- Button, Input, and Dialog components work correctly in all 6 monochrome themes
- Linter passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-transformation/02-04-SUMMARY.md`
</output>
