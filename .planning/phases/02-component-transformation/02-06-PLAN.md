---
phase: 02-component-transformation
plan: 06
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - components/ui/8bit/select.tsx
  - components/ui/8bit/slider.tsx
  - components/ui/8bit/calendar.tsx
  - components/ui/8bit/input-otp.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Select displays dropdown with thin border and terminal styling"
    - "Slider shows thin track with terminal-style thumb"
    - "Calendar displays dates in monospace grid with thin borders"
    - "Input OTP shows individual character boxes with thin borders"
  artifacts:
    - path: "components/ui/8bit/select.tsx"
      provides: "Terminal-style select with thin borders"
      exports: ["Select", "SelectGroup", "SelectValue", "SelectTrigger", "SelectContent", "SelectLabel", "SelectItem", "SelectSeparator", "SelectScrollUpButton", "SelectScrollDownButton"]
    - path: "components/ui/8bit/slider.tsx"
      provides: "Terminal slider with thin track"
      exports: ["Slider"]
    - path: "components/ui/8bit/calendar.tsx"
      provides: "Terminal calendar with monospace date grid"
      exports: ["Calendar"]
  key_links:
    - from: "components/ui/8bit/select.tsx"
      to: "components/ui/8bit/styles/protheus.css"
      via: "Phosphor glow and border utilities"
      pattern: "phosphor-.*glow|border-dos"
    - from: "components/ui/8bit/calendar.tsx"
      to: "components/ui/8bit/styles/protheus.css"
      via: "Monospace and border utilities"
      pattern: "protheus|border-dos"
---

<objective>
Transform 4 form control components (Select, Slider, Calendar, Input OTP) from 8bit pixel aesthetic to Protheus terminal aesthetic.

Purpose: Form controls are interactive elements that need clear terminal styling for input feedback. Select and Calendar are particularly complex and require careful transformation.

Output: 4 refactored form control components with terminal styling and thin borders.
</objective>

<execution_context>
@/Users/jubs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jubs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-transformation/02-RESEARCH.md
@components/ui/8bit/styles/protheus.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Transform Select and Slider components</name>
  <files>
    components/ui/8bit/select.tsx
    components/ui/8bit/slider.tsx
  </files>
  <action>
Transform Select (components/ui/8bit/select.tsx):
1. Remove pixel border patterns
2. Replace `retro` with `protheus`
3. Replace import to protheus.css
4. SelectTrigger:
   - border border-foreground bg-transparent
   - protheus uppercase text-sm
   - Add chevron indicator: use v or down arrow character
   - hover: phosphor-border-glow
   - focus: focus-glow
5. SelectContent (dropdown):
   - border border-foreground bg-background p-1
   - phosphor-border-glow
6. SelectItem:
   - px-2 py-1.5 protheus-normal text-sm
   - hover/focus: bg-foreground/10
   - Selected: bg-foreground text-background
7. SelectLabel: protheus uppercase text-xs text-muted-foreground
8. SelectSeparator: border-t border-foreground/30
9. SelectScrollUpButton/DownButton: use ^ and v characters
10. Keep Radix Select primitives for accessibility

Transform Slider (components/ui/8bit/slider.tsx):
1. Remove pixel border patterns
2. Replace `retro` with `protheus`
3. Track styling:
   - h-1 bg-foreground/20 border border-foreground/30 (thin track)
   - OR use ASCII representation [====----]
4. Range (filled part): bg-foreground (solid fill)
5. Thumb:
   - w-3 h-4 or h-3 w-3 square
   - border border-foreground bg-background
   - hover: phosphor-border-glow
   - focus: focus-glow
6. Alternative: ASCII slider [###|----] where | is thumb position
7. Add aria-valuetext for screen readers
8. Keep Radix Slider primitives
  </action>
  <verify>
Run: `npm run lint -- --quiet components/ui/8bit/select.tsx components/ui/8bit/slider.tsx`
Manual: Select dropdown has thin 1px borders
Manual: Slider track and thumb have thin borders
  </verify>
  <done>
Select and Slider display terminal aesthetic with thin borders and appropriate glow effects
  </done>
</task>

<task type="auto">
  <name>Task 2: Transform Calendar and Input OTP components</name>
  <files>
    components/ui/8bit/calendar.tsx
    components/ui/8bit/input-otp.tsx
  </files>
  <action>
Transform Calendar (components/ui/8bit/calendar.tsx):
1. Remove pixel border patterns
2. Replace `retro` with `protheus`
3. Calendar container: border border-foreground p-3
4. Navigation (prev/next month):
   - Use < > characters for arrows
   - border border-foreground size-7
   - hover: phosphor-border-glow
5. Month/year header: protheus uppercase text-sm phosphor-glow
6. Weekday headers: protheus uppercase text-xs text-muted-foreground
7. Day cells:
   - protheus text-sm
   - size-8 or size-9 (square grid)
   - hover: bg-foreground/10
   - selected: bg-foreground text-background
   - today: border border-foreground
   - outside month: text-muted-foreground/50
8. Focus states: focus-glow on focusable elements
9. Grid should align in monospace columns naturally
10. Keep react-day-picker functionality

Transform Input OTP (components/ui/8bit/input-otp.tsx):
1. Remove pixel border patterns
2. Replace `retro` with `protheus`
3. Container: inline-flex gap-2
4. Each input slot:
   - border border-foreground
   - size-10 or h-10 w-8
   - text-center protheus text-lg
   - focus: phosphor-border-glow
5. Separator between groups: text-muted-foreground, use - or . character
6. Caret indicator: terminal-cursor style (blinking block)
7. Filled state: phosphor-glow on text
8. Keep input-otp library functionality for paste handling
  </action>
  <verify>
Run: `npm run lint -- --quiet components/ui/8bit/calendar.tsx components/ui/8bit/input-otp.tsx`
Manual: Calendar grid aligns in monospace columns
Manual: Calendar day cells have thin borders on selection
Manual: OTP input slots have thin borders
  </verify>
  <done>
Calendar and Input OTP display terminal aesthetic with monospace alignment and thin borders
  </done>
</task>

</tasks>

<verification>
1. Run linter on all 4 modified files
2. Visual check: Select dropdown has thin 1px borders
3. Visual check: Slider track is thin line, thumb is small square
4. Visual check: Calendar has thin border, day grid aligns correctly
5. Visual check: OTP input shows individual boxes with thin borders
6. Keyboard test: Can navigate calendar with arrow keys
7. Keyboard test: Can select from dropdown with keyboard
8. Theme test: All borders follow theme color
</verification>

<success_criteria>
- All 4 form control components use thin 1px borders
- Select dropdown shows thin borders with glow on open
- Slider has thin track and square thumb
- Calendar displays in monospace grid with thin borders
- Input OTP shows individual character boxes
- Keyboard navigation works correctly
- Linter passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-transformation/02-06-SUMMARY.md`
</output>
