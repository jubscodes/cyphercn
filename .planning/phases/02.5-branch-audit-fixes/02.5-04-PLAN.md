---
phase: 02.5-branch-audit-fixes
plan: 04
type: execute
wave: 2
depends_on: ["02.5-02"]
files_modified:
  - components/ui/cypher/blocks/login-form.tsx
  - components/ui/cypher/blocks/login-form-2.tsx
  - components/ui/cypher/blocks/login-form-with-image.tsx
  - components/ui/cypher/blocks/chart.tsx
  - components/ui/cypher/blocks/chart-bar.tsx
  - components/ui/cypher/blocks/chart-area-step.tsx
  - components/ui/cypher/chart.tsx
autonomous: true

must_haves:
  truths:
    - "Login forms use terminal typography (cyphercn class), terminal copy, and no OAuth provider buttons"
    - "Login forms have rounded-none, thin border, terminal aesthetic"
    - "Chart blocks use terminal-appropriate colors from CSS variables (not default recharts colors)"
    - "Chart container/wrapper uses terminal border and cyphercn font"
    - "Build passes with no regressions"
  artifacts:
    - path: "components/ui/cypher/blocks/login-form.tsx"
      provides: "Terminal-styled login form"
    - path: "components/ui/cypher/blocks/login-form-2.tsx"
      provides: "Terminal-styled login form variant 2"
    - path: "components/ui/cypher/blocks/login-form-with-image.tsx"
      provides: "Terminal-styled login form with ASCII art"
    - path: "components/ui/cypher/blocks/chart.tsx"
      provides: "Terminal-styled area chart block"
    - path: "components/ui/cypher/blocks/chart-bar.tsx"
      provides: "Terminal-styled bar chart block"
    - path: "components/ui/cypher/blocks/chart-area-step.tsx"
      provides: "Terminal-styled step area chart block"
    - path: "components/ui/cypher/chart.tsx"
      provides: "Chart wrapper with terminal styling"
  key_links:
    - from: "components/ui/cypher/blocks/chart.tsx"
      to: "components/ui/cypher/chart.tsx"
      via: "ChartContainer import"
      pattern: "import.*ChartContainer"
---

<objective>
Restyle 3 login forms and 3 chart blocks with terminal aesthetic. Login forms get terminal copy and stripped OAuth. Charts get monochrome styling and terminal borders.

Purpose: The login forms and charts are the last components with zero terminal aesthetic — they look like generic modern UI and break the CypherCN identity.
Output: 6 restyled block files + 1 chart wrapper update, all with terminal aesthetic.
</objective>

<execution_context>
@/Users/jubs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jubs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@components/ui/cypher/button.tsx
@components/ui/cypher/card.tsx
@components/ui/cypher/input.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle 3 login forms with terminal aesthetic</name>
  <files>
    components/ui/cypher/blocks/login-form.tsx
    components/ui/cypher/blocks/login-form-2.tsx
    components/ui/cypher/blocks/login-form-with-image.tsx
  </files>
  <action>
  All 3 login forms need terminal aesthetic overhaul. They currently look like generic modern OAuth forms. Apply these changes to each:

  1. **Add CSS import:** `import "../styles/cyberpunk.css";` (if not already present)

  2. **Terminal copy:**
     - Title: "TERMINAL ACCESS" or "SYSTEM LOGIN" (not just "Login")
     - Description: "Enter credentials to authenticate" (not "Enter your email below to login to your account")
     - Button text: "[ AUTHENTICATE ]" (bracket notation per project conventions)
     - Links: "REQUEST ACCESS" instead of "Sign up", "FORGOT CREDENTIALS?" instead of "Forgot your password?"

  3. **Remove OAuth buttons** (GitHub, Google, etc.) — these are modern web patterns that don't fit a terminal. If the form has "Login with GitHub" / "Login with Google" type buttons, remove them entirely. Keep only the email/password form.

  4. **Terminal styling classes:**
     - Add `cyphercn` class to the wrapper div
     - Ensure Card already provides terminal border (it does from the Card component)
     - Labels should use `cyphercn` class for uppercase terminal font
     - Add `font-mono` where needed

  5. **login-form-with-image.tsx specifically:** The "image" portion should not reference a photo/image URL. Replace the image area with an ASCII art terminal graphic or a simple `cyphercn` branded panel. Use a div with `cyphercn` class containing ASCII art text like:
     ```
     ╔══════════════════╗
     ║  CYPHERCN v1.0   ║
     ║  SECURE TERMINAL  ║
     ╚══════════════════╝
     ```
     This keeps the split layout but replaces the photo with terminal-appropriate content.

  Read each file fully before editing to preserve the form structure and component imports. Only change styling/copy, not form logic.
  </action>
  <verify>Run `grep -l "cyphercn" components/ui/cypher/blocks/login-form.tsx components/ui/cypher/blocks/login-form-2.tsx components/ui/cypher/blocks/login-form-with-image.tsx` — all 3 should appear. Run `grep -i "github\|google\|oauth" components/ui/cypher/blocks/login-form*.tsx` — should return nothing.</verify>
  <done>All 3 login forms use terminal typography, terminal copy, bracket-notation buttons, no OAuth providers.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle chart blocks and chart wrapper with terminal aesthetic</name>
  <files>
    components/ui/cypher/blocks/chart.tsx
    components/ui/cypher/blocks/chart-bar.tsx
    components/ui/cypher/blocks/chart-area-step.tsx
    components/ui/cypher/chart.tsx
  </files>
  <action>
  **chart.tsx (base wrapper):**
  Read the full file first. The ChartContainer component wraps recharts. Add terminal styling:
  1. Ensure it imports cyberpunk.css
  2. The container should have `cyphercn-normal` class and `rounded-none`
  3. Chart tooltip should use `rounded-none border-foreground bg-background` for terminal look

  **Chart block files (chart.tsx, chart-bar.tsx, chart-area-step.tsx in blocks/):**
  These are vanilla recharts with default styling. Apply terminal aesthetic:

  1. **Add CSS import** if not present: `import "../styles/cyberpunk.css";`

  2. **Wrap in terminal container:** Add `cyphercn-normal` class to the outer wrapper

  3. **Monochrome chart colors:** The chart config colors should use CSS variables that respond to the active theme. Replace any hardcoded chart colors with:
     - `color: "hsl(var(--foreground))"` for primary data series
     - `color: "hsl(var(--foreground) / 0.5)"` for secondary data series
     This makes charts monochrome (matching the active terminal theme color).

  4. **Grid and axis styling:** Add props to CartesianGrid, XAxis, YAxis:
     - `CartesianGrid`: `strokeDasharray="3 3"` with `stroke="hsl(var(--foreground) / 0.15)"`
     - `XAxis`/`YAxis`: `tick={{ fill: 'hsl(var(--foreground) / 0.7)', fontSize: 11, fontFamily: 'IBM Plex Mono' }}` and `stroke="hsl(var(--foreground) / 0.3)"`

  5. **Chart areas/bars:** Use `fill="hsl(var(--foreground) / 0.2)"` for area fills and `stroke="hsl(var(--foreground))"` for lines. Bar charts use `fill="hsl(var(--foreground) / 0.6)"`.

  Read each file fully before editing. Preserve the data and chart structure — only change visual styling.
  </action>
  <verify>Run `grep -l "cyberpunk.css" components/ui/cypher/blocks/chart.tsx components/ui/cypher/blocks/chart-bar.tsx components/ui/cypher/blocks/chart-area-step.tsx` — all should appear. Run `grep "cyphercn" components/ui/cypher/blocks/chart.tsx` — should find matches. Run `grep "rounded-none" components/ui/cypher/chart.tsx` — should find match.</verify>
  <done>All chart blocks import cyberpunk.css, use monochrome theme-aware colors, have terminal typography. Chart wrapper has terminal borders.</done>
</task>

</tasks>

<verification>
- All login forms have terminal copy and no OAuth buttons
- All chart blocks use theme-aware monochrome colors
- All files import cyberpunk.css
- `npx next build` passes with no errors
</verification>

<success_criteria>
Login forms and charts are visually consistent with the CypherCN terminal aesthetic. No modern/generic UI patterns remain. Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-branch-audit-fixes/02.5-04-SUMMARY.md`
</output>
