---
phase: 02.5-branch-audit-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - registry.json
  - components/ui/cypher/carousel.tsx
  - components/ui/cypher/dropdown-menu.tsx
  - components/ui/cypher/skeleton.tsx
  - components/ui/cypher/menubar.tsx
autonomous: true

must_haves:
  truths:
    - "combo-box entry removed from registry.json (no component file exists)"
    - "terminal-log block added to registry.json with correct file paths"
    - "carousel registry title says 'Carousel' not 'Card'"
    - "alert registry description says 'alert' not 'switch'"
    - "No console.log statements in carousel.tsx"
    - "BitDropdownMenuContentProps spelled correctly in dropdown-menu.tsx"
    - "BitSkeletonProps is plural in skeleton.tsx"
    - "MenubarShortcut.displayName uses correct casing"
  artifacts:
    - path: "registry.json"
      provides: "Corrected registry entries"
    - path: "components/ui/cypher/carousel.tsx"
      provides: "Clean carousel without console.log"
    - path: "components/ui/cypher/dropdown-menu.tsx"
      provides: "Fixed typo in type name"
    - path: "components/ui/cypher/skeleton.tsx"
      provides: "Fixed plural Props type"
    - path: "components/ui/cypher/menubar.tsx"
      provides: "Fixed displayName casing"
  key_links: []
---

<objective>
Fix registry.json errors, remove debug console.log, and correct naming typos across 5 files.

Purpose: Clean up registry integrity issues and naming inconsistencies found during branch audit.
Output: Corrected registry.json, typo-free component type names, no debug logging.
</objective>

<execution_context>
@/Users/jubs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jubs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix registry.json — remove combo-box, add terminal-log, fix title/description typos</name>
  <files>registry.json</files>
  <action>
  In `registry.json`:

  1. **Remove the combo-box entry entirely** (lines ~922-945). There is no `components/ui/cypher/combo-box.tsx` file — this is a phantom registry entry.

  2. **Add terminal-log block entry.** The file `components/ui/cypher/blocks/terminal-log.tsx` exists but is not in the registry. Add this entry in the items array:
  ```json
  {
    "name": "terminal-log",
    "type": "registry:block",
    "title": "CypherCN Terminal Log",
    "description": "A terminal-styled scrolling log output with timestamps and level-based styling.",
    "registryDependencies": ["scroll-area", "card"],
    "categories": ["terminal"],
    "files": [
      {
        "path": "components/ui/cypher/blocks/terminal-log.tsx",
        "type": "registry:block",
        "target": "components/ui/cypher/blocks/terminal-log.tsx"
      },
      {
        "path": "components/ui/cypher/styles/cyberpunk.css",
        "type": "registry:component",
        "target": "components/ui/cypher/styles/cyberpunk.css"
      }
    ]
  }
  ```
  Check the actual imports in terminal-log.tsx and include any additional component files it imports (scroll-area.tsx, card.tsx, etc.) in the files array.

  3. **Fix carousel title:** Change `"title": "8-bit Card"` to `"title": "8-bit Carousel"` in the carousel entry.

  4. **Fix alert description:** Change `"description": "A simple 8-bit switch component"` to `"description": "A simple 8-bit alert component"` in the alert entry.
  </action>
  <verify>Run `node -e "const r = require('./registry.json'); const names = r.items.map(i=>i.name); console.log('combo-box:', names.includes('combo-box')); console.log('terminal-log:', names.includes('terminal-log')); const carousel = r.items.find(i=>i.name==='carousel'); console.log('carousel title:', carousel.title); const alert = r.items.find(i=>i.name==='alert'); console.log('alert desc:', alert.description);"` — should show combo-box: false, terminal-log: true, carousel title includes "Carousel", alert desc includes "alert".</verify>
  <done>combo-box removed, terminal-log added, carousel title says Carousel, alert description says alert</done>
</task>

<task type="auto">
  <name>Task 2: Fix naming typos in carousel, dropdown-menu, skeleton, menubar</name>
  <files>
    components/ui/cypher/carousel.tsx
    components/ui/cypher/dropdown-menu.tsx
    components/ui/cypher/skeleton.tsx
    components/ui/cypher/menubar.tsx
  </files>
  <action>
  1. **carousel.tsx** — Remove `console.log("scrolled prev")` on line 82 and `console.log("scrolled next")` on line 87. Delete those lines entirely, keeping the `api?.scrollPrev()` and `api?.scrollNext()` calls.

  2. **dropdown-menu.tsx** — Rename all occurrences of `BitDropownMenuContentProps` to `BitDropdownMenuContentProps` (add the missing 'd'). There are 3 occurrences: the interface declaration and two usages.

  3. **skeleton.tsx** — Rename `BitSkeletonProp` to `BitSkeletonProps` (add plural 's'). Both the interface declaration (line 7) and the function parameter (line 11).

  4. **menubar.tsx** — Change `MenubarShortcut.displayname` to `MenubarShortcut.displayName` (capital 'N') on line 348.
  </action>
  <verify>Run `grep -n "console.log" components/ui/cypher/carousel.tsx` (should return nothing), `grep -n "BitDropownMenu" components/ui/cypher/dropdown-menu.tsx` (should return nothing), `grep -n "BitSkeletonProp[^s]" components/ui/cypher/skeleton.tsx` (should return nothing), `grep -n "displayname" components/ui/cypher/menubar.tsx` (should return nothing — only displayName with capital N).</verify>
  <done>All 4 naming issues fixed: no console.log in carousel, BitDropdownMenuContentProps spelled correctly, BitSkeletonProps is plural, displayName has correct casing</done>
</task>

</tasks>

<verification>
- `node -e "JSON.parse(require('fs').readFileSync('registry.json','utf8'))"` succeeds (valid JSON)
- No `console.log` in carousel.tsx
- No typos in type/interface names
- `npx next build` passes (run at end of phase, not per-plan)
</verification>

<success_criteria>
Registry.json has no phantom entries, terminal-log is registered, all naming typos fixed, no debug logging in production code.
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-branch-audit-fixes/02.5-01-SUMMARY.md`
</output>
