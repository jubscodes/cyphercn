---
phase: 02.5-branch-audit-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui/cypher/health-bar.tsx
  - components/ui/cypher/mana-bar.tsx
  - components/ui/cypher/xp-bar.tsx
  - components/ui/cypher/enemy-health-display.tsx
  - components/ui/cypher/blocks/friend-list.tsx
autonomous: true

must_haves:
  truths:
    - "health-bar.tsx has no nested props?: CypherProgressProps field — uses flat props only"
    - "mana-bar.tsx has no nested props?: CypherProgressProps field — uses flat props only"
    - "xp-bar.tsx has no nested props?: CypherProgressProps field — uses flat props only"
    - "enemy-health-display.tsx passes progressBg as a flat prop, not as props={{ progressBg }}"
    - "xp-bar.tsx uses theme-aware color instead of hardcoded text-black"
    - "friend-list.tsx uses CSS custom properties or theme-aware classes instead of hardcoded hex colors"
  artifacts:
    - path: "components/ui/cypher/health-bar.tsx"
      provides: "Health bar with flat props interface"
    - path: "components/ui/cypher/mana-bar.tsx"
      provides: "Mana bar with flat props interface"
    - path: "components/ui/cypher/xp-bar.tsx"
      provides: "XP bar with flat props and theme-aware text"
    - path: "components/ui/cypher/enemy-health-display.tsx"
      provides: "Enemy display passing flat props to HealthBar"
    - path: "components/ui/cypher/blocks/friend-list.tsx"
      provides: "Friend list using theme-aware status colors"
  key_links:
    - from: "components/ui/cypher/enemy-health-display.tsx"
      to: "components/ui/cypher/health-bar.tsx"
      via: "HealthBar component usage"
      pattern: "<HealthBar"
---

<objective>
Fix the nested props anti-pattern in health/mana/xp bars, fix enemy-health-display nested prop passing, replace hardcoded colors with theme-aware alternatives.

Purpose: Ensure consistent flat props pattern across bar components and theme-aware colors for light/dark mode compatibility.
Output: 5 updated files with clean props interfaces and theme-aware colors.
</objective>

<execution_context>
@/Users/jubs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jubs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@components/ui/cypher/progress.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix nested props anti-pattern in health-bar, mana-bar, xp-bar and fix enemy-health-display usage</name>
  <files>
    components/ui/cypher/health-bar.tsx
    components/ui/cypher/mana-bar.tsx
    components/ui/cypher/xp-bar.tsx
    components/ui/cypher/enemy-health-display.tsx
  </files>
  <action>
  **health-bar.tsx:**
  Remove the `props?: CypherProgressProps` field from the `HealthBarProps` interface. The interface should only have flat props that get passed through. Current interface has both `variant` and `props` which is confusing. The component already destructures `variant` and `value` and passes them directly — the `props` field is unused dead code. Remove it.

  Updated interface:
  ```tsx
  interface HealthBarProps extends React.ComponentProps<"div"> {
    className?: string;
    variant?: CypherProgressProps["variant"];
    value?: number;
    progressBg?: string;
  }
  ```
  Add `progressBg` as a flat prop (default `"bg-red-500"`) and pass it to `<Progress progressBg={progressBg} />`.

  **mana-bar.tsx:**
  Same fix as health-bar. Remove `props?: CypherProgressProps`. Add `progressBg` as flat prop (default `"bg-blue-500"`).

  **xp-bar.tsx:**
  Same fix. Remove `props?: CypherProgressProps`. Add `progressBg` as flat prop (default `"bg-yellow-500"`).
  Also fix the hardcoded `text-black` on line 35 — replace with `text-foreground` for theme awareness. The `[text-shadow]` and `drop-shadow` on the LEVEL UP text should also use theme-aware colors: replace `#fff` references with `currentColor` or use `text-background` class instead of `text-black`.

  **enemy-health-display.tsx:**
  On line 100, change `props={{ progressBg: healthBarColor }}` to just `progressBg={healthBarColor}`. The HealthBar component now accepts `progressBg` as a flat prop, so pass it directly.
  </action>
  <verify>Run `grep -n "props?:" components/ui/cypher/health-bar.tsx components/ui/cypher/mana-bar.tsx components/ui/cypher/xp-bar.tsx` — should return nothing. Run `grep -n "props={{" components/ui/cypher/enemy-health-display.tsx` — should return nothing. Run `grep -n "text-black" components/ui/cypher/xp-bar.tsx` — should return nothing.</verify>
  <done>All bar components use flat props. Enemy display passes props flatly. XP bar uses theme-aware text color.</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded hex colors in friend-list with theme-aware classes</name>
  <files>components/ui/cypher/blocks/friend-list.tsx</files>
  <action>
  The friend-list.tsx uses hardcoded neon hex colors for status indicators:
  - `#ff0055` (offline/red) — replace with `bg-destructive border-destructive shadow-destructive/50` or use `bg-red-500 border-red-500` (these are Tailwind palette colors that work across themes).
  - `#00ff00` (online/green) — replace with `bg-green-500 border-green-500`
  - `#ffea00` (away/yellow) — replace with `bg-yellow-500 border-yellow-500`

  The `shadow-[0_0_6px_...]` glow effects should use the same Tailwind color: `shadow-red-500/50`, `shadow-green-500/50`, `shadow-yellow-500/50`.

  This preserves the visual intent (colored status dots with glow) while using Tailwind's color system instead of bypassing CSS variables with raw hex. The glow effect uses the phosphor glow pattern established in cyberpunk.css.
  </action>
  <verify>Run `grep -n "#ff0055\|#00ff00\|#ffea00" components/ui/cypher/blocks/friend-list.tsx` — should return nothing.</verify>
  <done>Friend list uses Tailwind color classes instead of hardcoded hex values. Status indicators remain visually distinct across themes.</done>
</task>

</tasks>

<verification>
- No `props?:` field in any bar component interface
- No `props={{` in enemy-health-display
- No `text-black` in xp-bar
- No hardcoded hex colors in friend-list
</verification>

<success_criteria>
All bar components use flat props pattern. Enemy display passes progressBg directly. XP bar text is theme-aware. Friend list status colors use Tailwind classes.
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-branch-audit-fixes/02.5-03-SUMMARY.md`
</output>
