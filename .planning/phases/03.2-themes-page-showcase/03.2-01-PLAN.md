---
phase: 03.2-themes-page-showcase
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/examples/component-showcase.tsx
  - app/themes/page.tsx
autonomous: true

must_haves:
  truths:
    - "User sees distinct Command Center section with terminal components"
    - "User sees distinct Cyber Games section with game block components"
    - "Sections have clear headings and descriptions"
    - "Theme switcher still works across all components"
  artifacts:
    - path: "components/examples/component-showcase.tsx"
      provides: "Section-organized component showcase"
      contains: "CommandCenterSection"
    - path: "app/themes/page.tsx"
      provides: "Updated page structure with sections"
      min_lines: 30
  key_links:
    - from: "app/themes/page.tsx"
      to: "components/examples/component-showcase.tsx"
      via: "import ComponentShowcase"
      pattern: "ComponentShowcase"
    - from: "components/examples/component-showcase.tsx"
      to: "@/components/ui/cypher/*"
      via: "component imports"
      pattern: "from.*cypher"
---

<objective>
Refactor the /themes page component showcase to organize components into thematic sections, starting with Command Center and Cyber Games.

Purpose: Transform the flat 4-column grid into organized sections that better demonstrate the CypherCN aesthetic and help users discover components by use case.

Output: Refactored component-showcase.tsx with section-based organization and first two thematic sections implemented.
</objective>

<execution_context>
@/Users/jubs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jubs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.2-themes-page-showcase/03.2-RESEARCH.md
@components/examples/component-showcase.tsx
@app/themes/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor component-showcase.tsx with section architecture</name>
  <files>components/examples/component-showcase.tsx</files>
  <action>
Refactor component-showcase.tsx to use section-based organization:

1. Create reusable Section component pattern:
```tsx
function Section({ title, description, children }: {
  title: string;
  description: string;
  children: React.ReactNode
}) {
  return (
    <section className="py-8">
      <div className="mb-6">
        <h2 className="cyphercn text-xl font-bold">{title}</h2>
        <p className="text-muted-foreground text-sm">{description}</p>
      </div>
      <div className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3">
        {children}
      </div>
    </section>
  );
}
```

2. Implement Command Center section with these components:
   - Alert (info and destructive variants)
   - Command palette (CommandExample)
   - Input, Textarea (form inputs)
   - Menubar (File/Edit menus)
   - Card with stats display

3. Implement Cyber Games section with these block components:
   - MainMenu
   - GameOver
   - DifficultySelect
   - ChapterIntro
   - GameProgress
   - Dialogue
   - EnemyHealthDisplay, ManaBar
   - AudioSettings

4. Keep all existing imports but reorganize them into the section structure.

5. Use responsive grid: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` for sections.

6. Wrap Card around individual component showcases for visual consistency.

Do NOT:
- Add "use client" directive (keep as Server Component)
- Remove any existing components (reorganize them)
- Change import paths from cypher namespace
- Use hardcoded colors (keep CSS variable-based theming)
  </action>
  <verify>
Run `npm run build` to verify no TypeScript/import errors.
Check that component-showcase.tsx has Section function and CommandCenterSection/CyberGamesSection structure.
  </verify>
  <done>
component-showcase.tsx refactored with:
- Reusable Section component
- Command Center section with terminal components
- Cyber Games section with game blocks
- Responsive grid layout per section
  </done>
</task>

<task type="auto">
  <name>Task 2: Update themes page with improved header and structure</name>
  <files>app/themes/page.tsx</files>
  <action>
Update the themes page to better introduce the showcase:

1. Update the page header text to be more terminal-themed:
   - Title: "TERMINAL THEMES" (using cyphercn class)
   - Subtitle: "Select a phosphor color scheme for your command center interface."

2. Keep the existing structure:
   - ThemeSelector stays in place (Client Component island)
   - ComponentShowcase import and rendering stays the same

3. Add semantic section wrapper around ComponentShowcase:
```tsx
<main className="p-5">
  <ComponentShowcase />
</main>
```

4. Ensure metadata.description uses terminal terminology (already correct).

Do NOT:
- Add "use client" to the page
- Modify ThemeSelector component
- Change the import path for ComponentShowcase
  </action>
  <verify>
Run `npm run dev` and visit `/themes` page.
Verify header displays "TERMINAL THEMES" with subtitle.
Verify sections are visible and organized.
  </verify>
  <done>
Themes page updated with:
- Terminal-themed header text
- Proper semantic structure with main element
- Component showcase renders with section organization
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes without errors
2. `/themes` page loads with section-based layout
3. Command Center section displays terminal components
4. Cyber Games section displays game block components
5. Theme switcher changes colors across all sections
6. Page is responsive (test at 375px, 768px, 1280px widths)
</verification>

<success_criteria>
- Component showcase refactored from flat grid to section-based organization
- Command Center section shows Alert, Command, Input, Menubar, Cards
- Cyber Games section shows MainMenu, GameOver, Dialogue, etc.
- Theme switching works across all components
- No build errors
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-themes-page-showcase/03.2-01-SUMMARY.md`
</output>
