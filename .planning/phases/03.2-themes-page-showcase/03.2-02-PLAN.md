---
phase: 03.2-themes-page-showcase
plan: 02
type: execute
wave: 2
depends_on: ["03.2-01"]
files_modified:
  - components/examples/component-showcase.tsx
autonomous: false

must_haves:
  truths:
    - "User sees distinct Base Components section with UI primitives"
    - "User sees distinct Dashboard section with data visualization components"
    - "All four sections display in logical order"
    - "Page serves as visual portfolio of CypherCN aesthetic"
    - "Responsive layout works on mobile and desktop"
  artifacts:
    - path: "components/examples/component-showcase.tsx"
      provides: "Complete 4-section component showcase"
      contains: "BaseComponentsSection"
  key_links:
    - from: "components/examples/component-showcase.tsx"
      to: "@/components/ui/cypher/*"
      via: "component imports"
      pattern: "Button|Card|Badge|Select|Checkbox"
---

<objective>
Complete the themes page showcase by implementing Base Components and Dashboard sections, then verify the complete visual portfolio.

Purpose: Finish the thematic organization to demonstrate the full breadth of CypherCN components, making the library's value immediately apparent to visitors.

Output: Complete 4-section component showcase with visual verification.
</objective>

<execution_context>
@/Users/jubs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jubs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.2-themes-page-showcase/03.2-RESEARCH.md
@.planning/phases/03.2-themes-page-showcase/03.2-01-SUMMARY.md
@components/examples/component-showcase.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Base Components section</name>
  <files>components/examples/component-showcase.tsx</files>
  <action>
Add Base Components section to component-showcase.tsx:

1. Section header:
   - Title: "Base Components"
   - Description: "Core UI primitives for building terminal interfaces."

2. Include these components (wrap each in Card for consistency):
   - Button (default variant)
   - DrawerExample
   - DropdownMenu with trigger
   - Input with placeholder
   - Select with options
   - DatePicker
   - Checkbox examples (from existing Game Options card)
   - Spinner variants (diamond and classic)
   - Badge examples (Level 42, Warrior, etc.)
   - Tabs example (Items/Skills/Stats)
   - Label components

3. Use responsive grid: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`

4. Each component should be wrapped in a Card with CardHeader (component name) and CardContent (the component).

5. Position this section AFTER Cyber Games, BEFORE Dashboard.

Do NOT:
- Remove any components that were in the original showcase
- Add new imports that don't already exist
- Use "use client" directive
  </action>
  <verify>
Run `npm run build` to verify no errors.
Grep for "Base Components" in component-showcase.tsx.
  </verify>
  <done>
Base Components section implemented with:
- Button, Input, Select, Checkbox, Badge, Tabs
- Drawer, Dropdown, DatePicker
- Spinner variants
- All wrapped in Cards with component names
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Dashboard section</name>
  <files>components/examples/component-showcase.tsx</files>
  <action>
Add Dashboard section to component-showcase.tsx:

1. Section header:
   - Title: "Dashboard"
   - Description: "Data visualization and monitoring components."

2. Include these components:
   - Card with "Active Now" stats (+573, activity icon)
   - Card with "Subscriptions" stats (+2350, user icon)
   - Card with "Warriors" stats (+100)
   - Card with "Wizards" stats (+1000)
   - Card with "Product Details" form (Name + Description)

3. Use responsive grid: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4` (4 columns for dashboard cards)

4. Keep the existing stat cards but move them into this section.

5. Position this section LAST (after Base Components).

6. Final section order in the file:
   1. Command Center
   2. Cyber Games
   3. Base Components
   4. Dashboard

Do NOT:
- Create new stat card designs (reuse existing)
- Change card styling
- Add chart components that don't exist yet
  </action>
  <verify>
Run `npm run build` to verify no errors.
Grep for "Dashboard" in component-showcase.tsx.
  </verify>
  <done>
Dashboard section implemented with:
- Active Now, Subscriptions, Warriors, Wizards stat cards
- Product Details form card
- 4-column responsive grid for dashboard layout
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete themes page with 4 thematic sections: Command Center, Cyber Games, Base Components, Dashboard</what-built>
  <how-to-verify>
1. Run `npm run dev` and visit http://localhost:3000/themes
2. Verify page structure:
   - Header shows "TERMINAL THEMES" with phosphor subtitle
   - Theme selector is visible and functional
   - Four sections are clearly labeled
3. Test Command Center section:
   - Shows Alert, Command, Input, Menubar components
   - Terminal aesthetic applied
4. Test Cyber Games section:
   - Shows MainMenu, GameOver, Dialogue, etc.
   - Game blocks display correctly
5. Test Base Components section:
   - Shows Button, Select, Checkbox, Badge, Tabs, etc.
   - Components wrap in Cards with labels
6. Test Dashboard section:
   - Shows stat cards in grid layout
   - Product Details form visible
7. Test theme switching:
   - Click each theme (green, amber, cyan, red, white, orange)
   - Verify ALL sections change color
8. Test responsiveness:
   - Resize browser to mobile width (375px)
   - Verify single-column layout, no horizontal scroll
   - Resize to tablet (768px) - verify 2-column sections
   - Resize to desktop (1280px) - verify 3-4 column sections
  </how-to-verify>
  <resume-signal>Type "approved" if the page looks good, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` passes
2. All 4 sections visible with proper headers
3. Section order: Command Center -> Cyber Games -> Base Components -> Dashboard
4. Theme switching affects all components
5. Responsive layout works at all breakpoints
6. No components lost from original showcase
</verification>

<success_criteria>
- 4 thematic sections implemented and organized
- Each section has title, description, and component grid
- Theme switcher works across all sections
- Responsive layout verified on mobile/tablet/desktop
- Human verification confirms visual quality
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-themes-page-showcase/03.2-02-SUMMARY.md`
</output>
