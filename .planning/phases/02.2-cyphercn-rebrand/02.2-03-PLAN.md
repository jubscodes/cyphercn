---
phase: 02.2-cyphercn-rebrand
plan: 03
type: execute
wave: 2
depends_on: ["02.2-01", "02.2-02"]
files_modified:
  - .planning/PROJECT.md
  - .planning/STATE.md
  - registry.json
  - components.json
  - README.md
  - contributing.md
  - app/rss.xml/route.ts
autonomous: true

must_haves:
  truths:
    - "PROJECT.md refers to CypherCN throughout"
    - "registry.json uses cyphercn.com URLs"
    - "Full build passes with no broken references"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Project documentation"
      contains: "CypherCN"
    - path: "registry.json"
      provides: "Component registry configuration"
      contains: "cyphercn.com"
  key_links:
    - from: "registry.json"
      to: "cyphercn.com"
      via: "URL reference"
      pattern: "cyphercn\\.com"
---

<objective>
Update planning docs, registry configuration, and project files with CypherCN branding.

Purpose: Complete the rebrand by updating all remaining files that reference the old names and ensure the build passes.

Output: Planning docs, registry config, and project files all use CypherCN branding; verified build.
</objective>

<execution_context>
@/Users/jubs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jubs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.2-cyphercn-rebrand/02.2-01-SUMMARY.md
@.planning/phases/02.2-cyphercn-rebrand/02.2-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update planning documentation</name>
  <files>.planning/PROJECT.md, .planning/STATE.md</files>
  <action>
    In .planning/PROJECT.md:
    - Change title from "# CyberCN" to "# CypherCN"
    - Replace "CyberCN is a cyberpunk" with "CypherCN is a cyberpunk"
    - Replace all other "CyberCN" references with "CypherCN"
    - Keep the project description accurate
    - Update @cybercn/ to @cyphercn/ if present
    - Update any 8bitcn.com registry URLs to cyphercn.com

    In .planning/STATE.md:
    - Replace "CyberCN" with "CypherCN" in decisions section
    - Update any @cybercn/ to @cyphercn/
    - Update any 8bitcn.com registry references to cyphercn.com
    - Keep GitHub URLs unchanged (TheOrcDev/8bitcn-ui)
  </action>
  <verify>
    Run `grep -i "cybercn" .planning/PROJECT.md .planning/STATE.md` returns no matches.
    Run `grep "8bitcn.com" .planning/PROJECT.md .planning/STATE.md` returns no registry URL matches.
  </verify>
  <done>Planning docs use CypherCN branding throughout.</done>
</task>

<task type="auto">
  <name>Task 2: Update registry and config files</name>
  <files>registry.json, components.json</files>
  <action>
    In registry.json:
    - Replace any 8bitcn.com URLs with cyphercn.com for registry endpoints
    - Keep GitHub URLs unchanged

    In components.json:
    - Update registry URL from 8bitcn.com to cyphercn.com
    - Update @cybercn/ to @cyphercn/ if present
  </action>
  <verify>
    Run `grep "8bitcn.com" registry.json components.json` returns no matches (for registry URLs).
    Run `grep "cyphercn.com" registry.json components.json` shows the new URLs.
  </verify>
  <done>Registry configuration uses cyphercn.com domain.</done>
</task>

<task type="auto">
  <name>Task 3: Update project files</name>
  <files>README.md, contributing.md, app/rss.xml/route.ts</files>
  <action>
    In README.md:
    - Replace any 8bitcn.com registry URLs with cyphercn.com
    - Keep GitHub URLs (github.com/TheOrcDev/8bitcn-ui) unchanged
    - Update any @cybercn/ to @cyphercn/

    In contributing.md:
    - Replace any 8bitcn.com registry URLs with cyphercn.com
    - Keep GitHub URLs unchanged

    In app/rss.xml/route.ts:
    - Replace any 8bitcn.com URLs with cyphercn.com
    - Update site metadata if it references CyberCN
  </action>
  <verify>
    Run `grep "8bitcn.com" README.md contributing.md app/rss.xml/route.ts` returns no registry URL matches.
  </verify>
  <done>Project files use cyphercn.com for registry URLs.</done>
</task>

<task type="auto">
  <name>Task 4: Verify build and run final checks</name>
  <files>N/A (verification task)</files>
  <action>
    Run build verification:
    1. `npm run build` - must complete without errors
    2. Comprehensive grep checks:
       - `grep -ri "cybercn" content/ components/ lib/ app/ --include="*.tsx" --include="*.ts" --include="*.css" --include="*.mdx"` - should return no matches except intentional GitHub references
       - `grep -ri "@cybercn/" .` - should return no matches
       - `grep -ri "protheus" components/ lib/ app/globals.css` - should return no matches

    Fix any remaining references found during verification.
  </action>
  <verify>
    Build completes successfully.
    No stray CyberCN, @cybercn/, or protheus references remain.
  </verify>
  <done>Build passes, all branding consistent with CypherCN.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `grep -ri "@cybercn/" .` returns no matches
3. `grep -ri "protheus" components/ lib/ app/globals.css` returns no matches
4. `grep "8bitcn.com" registry.json components.json content/docs/index.mdx` returns no registry URL matches
5. All MDX files render correctly (verified by build)
</verification>

<success_criteria>
- Planning docs updated with CypherCN branding
- Registry configuration uses cyphercn.com
- Project files use cyphercn.com for registry
- Build passes without errors
- No stray old branding references remain
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-cyphercn-rebrand/02.2-03-SUMMARY.md`
</output>
