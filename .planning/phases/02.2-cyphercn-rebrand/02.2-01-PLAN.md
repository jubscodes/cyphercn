---
phase: 02.2-cyphercn-rebrand
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/themes.ts
  - components/active-theme.tsx
  - app/globals.css
  - components/ui/8bit/styles/protheus.css
  - components/installation-commands.tsx
autonomous: true

must_haves:
  truths:
    - "Theme selector shows 'cyphercn' instead of 'protheus'"
    - "Components render with .theme-cyphercn class"
    - "@cyphercn/ namespace appears in all installation commands"
  artifacts:
    - path: "lib/themes.ts"
      provides: "Theme.CypherCN constant"
      contains: "CypherCN: \"cyphercn\""
    - path: "app/globals.css"
      provides: ".theme-cyphercn CSS rules"
      contains: ".theme-cyphercn"
    - path: "components/ui/8bit/styles/cyphercn.css"
      provides: "Terminal typography classes"
      contains: ".cyphercn"
  key_links:
    - from: "components/active-theme.tsx"
      to: "lib/themes.ts"
      via: "DEFAULT_THEME import"
      pattern: "Theme\\.CypherCN"
---

<objective>
Rename the Protheus theme to CypherCN and update all theme system references.

Purpose: Establish brand consistency by renaming the terminal theme from "Protheus" to "CypherCN" across the theme system, CSS, and component styling.

Output: Updated theme system with CypherCN branding, renamed CSS file, and updated installation command namespace.
</objective>

<execution_context>
@/Users/jubs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jubs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename Protheus theme in theme system</name>
  <files>lib/themes.ts, components/active-theme.tsx</files>
  <action>
    In lib/themes.ts:
    - Change `Protheus: "protheus"` to `CypherCN: "cyphercn"` in the Theme const
    - Update the theme entry name from `Theme.Protheus` to `Theme.CypherCN`
    - Update comment from "Theme defined in globals.css .theme-protheus" to ".theme-cyphercn"

    In components/active-theme.tsx:
    - Change `DEFAULT_THEME = Theme.Protheus` to `DEFAULT_THEME = Theme.CypherCN`
  </action>
  <verify>
    Run `grep -r "Protheus" lib/themes.ts components/active-theme.tsx` returns no matches.
    Run `grep -r "CypherCN" lib/themes.ts components/active-theme.tsx` shows the new references.
  </verify>
  <done>Theme system uses CypherCN name throughout, no Protheus references remain.</done>
</task>

<task type="auto">
  <name>Task 2: Rename CSS classes from protheus to cyphercn</name>
  <files>app/globals.css, components/ui/8bit/styles/protheus.css</files>
  <action>
    In app/globals.css:
    - Replace all `.theme-protheus` with `.theme-cyphercn`
    - This includes variant classes like `.theme-protheus.variant-green`, `.theme-protheus.variant-amber`, etc.
    - Update high-contrast and forced-colors selectors

    Rename protheus.css to cyphercn.css:
    - Rename file from `components/ui/8bit/styles/protheus.css` to `components/ui/8bit/styles/cyphercn.css`
    - Replace all `.protheus` class references with `.cyphercn`
    - Replace all `.protheus-normal` class references with `.cyphercn-normal`
    - Update the file header comment from "PROTHEUS.CSS" to "CYPHERCN.CSS"
  </action>
  <verify>
    Run `grep -r "protheus" app/globals.css components/ui/8bit/styles/` returns no matches (case insensitive).
    Run `ls components/ui/8bit/styles/cyphercn.css` confirms file exists.
  </verify>
  <done>All CSS uses cyphercn naming, protheus.css renamed to cyphercn.css.</done>
</task>

<task type="auto">
  <name>Task 3: Update component class references</name>
  <files>components/ui/8bit/*.tsx (all files using protheus/protheus-normal classes)</files>
  <action>
    Search all component files in components/ui/8bit/ that reference:
    - `protheus` class -> replace with `cyphercn`
    - `protheus-normal` class -> replace with `cyphercn-normal`

    These files include: button.tsx, alert.tsx, toggle-group.tsx, separator.tsx, input-otp.tsx,
    toast.tsx, progress.tsx, scroll-area.tsx, carousel.tsx, calendar.tsx, resizable.tsx,
    command.tsx, slider.tsx, select.tsx, popover.tsx, drawer.tsx, context-menu.tsx,
    dropdown-menu.tsx, menubar.tsx, navigation-menu.tsx, sheet.tsx, pagination.tsx,
    alert-dialog.tsx, breadcrumb.tsx, dialog.tsx, tabs.tsx, hover-card.tsx, tooltip.tsx,
    avatar.tsx, kbd.tsx, collapsible.tsx, accordion.tsx, skeleton.tsx, badge.tsx, label.tsx,
    table.tsx, toggle.tsx, switch.tsx, radio-group.tsx, checkbox.tsx, card.tsx, textarea.tsx,
    input.tsx, and block components in components/ui/8bit/blocks/

    Also update components/ui/protheus/* files similarly.
  </action>
  <verify>
    Run `grep -rn "protheus" components/ui/8bit/ components/ui/protheus/` returns no matches.
  </verify>
  <done>All component files use cyphercn/cyphercn-normal class names.</done>
</task>

<task type="auto">
  <name>Task 4: Update InstallationCommands namespace</name>
  <files>components/installation-commands.tsx</files>
  <action>
    Replace all instances of `@cybercn/` with `@cyphercn/` in the installation commands.
    This affects:
    - pnpm command
    - npm command
    - yarn command
    - bun command
    - All display text showing the commands
  </action>
  <verify>
    Run `grep "@cybercn" components/installation-commands.tsx` returns no matches.
    Run `grep "@cyphercn" components/installation-commands.tsx` shows 8+ matches.
  </verify>
  <done>InstallationCommands uses @cyphercn/ namespace throughout.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `grep -ri "protheus" lib/ components/ app/globals.css` returns no matches
3. `grep -ri "@cybercn" components/installation-commands.tsx` returns no matches
4. CSS file exists at components/ui/8bit/styles/cyphercn.css
</verification>

<success_criteria>
- All Protheus references replaced with CypherCN in theme system
- All .protheus CSS classes renamed to .cyphercn
- All protheus/protheus-normal component classes renamed to cyphercn/cyphercn-normal
- InstallationCommands uses @cyphercn/ namespace
- Build passes successfully
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-cyphercn-rebrand/02.2-01-SUMMARY.md`
</output>
