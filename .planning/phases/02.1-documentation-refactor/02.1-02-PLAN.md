---
phase: 02.1-documentation-refactor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - content/docs/components/*.mdx
autonomous: true

must_haves:
  truths:
    - "All component MDX descriptions say 'terminal-styled' not '8-bit'"
    - "All CopyCommandButton commands reference @cybercn/"
    - "All ComponentPreview titles use CyberCN branding"
  artifacts:
    - path: "content/docs/components/button.mdx"
      provides: "Button component documentation"
      contains: "@cybercn/"
    - path: "content/docs/components/input.mdx"
      provides: "Input component documentation"
      contains: "terminal-styled"
  key_links:
    - from: "content/docs/components/*.mdx"
      to: "components/copy-command-button.tsx"
      via: "CopyCommandButton with copyCommand prop"
      pattern: "CopyCommandButton"
---

<objective>
Update all 57 component MDX files to use CyberCN branding and terminology.

Purpose: Component docs are the primary user touchpoint - they must reflect the new terminal aesthetic identity.
Output: All component documentation uses CyberCN/terminal-styled terminology with @cybercn/ registry namespace.
</objective>

<execution_context>
@/Users/jubs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jubs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02.1-documentation-refactor/02.1-RESEARCH.md
@content/docs/components/button.mdx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Batch update component MDX frontmatter descriptions</name>
  <files>content/docs/components/*.mdx (57 files)</files>
  <action>
Update the `description` field in YAML frontmatter for all component MDX files.

**Pattern to find:**
```
description: Displays an 8-bit
description: An 8-bit
description: 8-bit
```

**Replace with:**
```
description: A terminal-styled
```

Process all 57 files in content/docs/components/. Use find-replace or sed patterns.

Example transformation:
- `description: Displays an 8-bit button component.` -> `description: A terminal-styled button component.`
- `description: An 8-bit accordion component.` -> `description: A terminal-styled accordion component.`

Note: The title field stays unchanged (e.g., `title: Button` remains `title: Button`).
  </action>
  <verify>
Run: `grep -l "8-bit" content/docs/components/*.mdx | wc -l` - should return 0 (no files with 8-bit in frontmatter)
Run: `grep -c "terminal-styled" content/docs/components/*.mdx | grep -v ":0$" | wc -l` - should be ~57 files
  </verify>
  <done>All component MDX files have descriptions using "terminal-styled" terminology</done>
</task>

<task type="auto">
  <name>Task 2: Batch update CopyCommandButton and ComponentPreview</name>
  <files>content/docs/components/*.mdx (57 files)</files>
  <action>
Update CopyCommandButton commands and ComponentPreview titles in all component MDX files.

**CopyCommandButton pattern:**
Find: `@8bitcn/`
Replace: `@cybercn/`

This appears in both `copyCommand` and `command` props:
```mdx
<CopyCommandButton
  copyCommand="pnpm dlx shadcn@latest add @8bitcn/button"
  command="pnpm dlx shadcn@latest add @8bitcn/button"
/>
```

**ComponentPreview pattern:**
Find: `title="8-bit `
Replace: `title="CyberCN `

Example:
- `<ComponentPreview title="8-bit button component"` -> `<ComponentPreview title="CyberCN Button"`
- Capitalize the component name in the title for consistency (e.g., "CyberCN Button" not "CyberCN button component")

Process all 57 files systematically.
  </action>
  <verify>
Run: `grep -r "@8bitcn/" content/docs/components/*.mdx | wc -l` - should return 0
Run: `grep -r "@cybercn/" content/docs/components/*.mdx | wc -l` - should be >100 (2 per file)
Run: `grep -r 'title="8-bit' content/docs/components/*.mdx | wc -l` - should return 0
  </verify>
  <done>All CopyCommandButton commands use @cybercn/ and all ComponentPreview titles use CyberCN branding</done>
</task>

</tasks>

<verification>
- [ ] No component MDX files contain "8-bit" in description
- [ ] No component MDX files contain "@8bitcn/" in CopyCommandButton
- [ ] All ComponentPreview titles start with "CyberCN"
- [ ] `pnpm build` succeeds
</verification>

<success_criteria>
1. Zero occurrences of "8-bit" in component docs (except potentially in code comments)
2. All @8bitcn/ references replaced with @cybercn/
3. ComponentPreview titles use CyberCN branding
4. Build passes without MDX errors
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-documentation-refactor/02.1-02-SUMMARY.md`
</output>
